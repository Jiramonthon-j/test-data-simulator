# End-to-End Test Checklist — Intelligent Test Data Simulator

เช็กลิสต์ทดสอบทุก flow หลักก่อนส่งงานจริง (เทียบกับ `IMPROVEMENT_CHECKLIST.md` ข้อ 4 / ข้อ 9 เดิม) ทดสอบบนสภาพแวดล้อมปัจจุบัน (backend เวอร์ชัน v65) ให้ติ๊ก `[x]` ทีละข้อเมื่อทดสอบผ่านจริง ถ้าเจอปัญหาให้จดไว้ในช่อง "หมายเหตุ" ท้ายแต่ละหมวดแทนที่จะติ๊กผ่านไปก่อน

---

## 1. สมัครสมาชิก (Register)

- [x] สมัครด้วยอีเมล @gmail.com ที่ถูกต้อง + รหัสผ่านตรงเงื่อนไข (≥6 ตัว, มีพิมพ์ใหญ่/เล็ก, มีอักขระพิเศษ) → สมัครสำเร็จ, ไปหน้า login
- [x] สมัครด้วยอีเมลโดเมนอื่น (เช่น `test@yahoo.com`) → ถูกปฏิเสธทั้งฝั่งหน้าเว็บและ backend
- [x] สมัครด้วยอีเมลพิมพ์โดเมนผิด (เช่น `test@gamil.com`) → ถูกปฏิเสธ
- [x] ไม่ติ๊กยอมรับนโยบายความเป็นส่วนตัวก่อนสมัคร → กดสมัครไม่ได้/ขึ้นเตือน
- [x] รหัสผ่านไม่ตรงเงื่อนไข (สั้นเกินไป/ไม่มีอักขระพิเศษ) → ถูกปฏิเสธพร้อมข้อความบอกเงื่อนไข
- [x] สมัครด้วย username ที่มีอยู่แล้ว → ถูกปฏิเสธ
- [x] พิมพ์ username ที่มีอยู่แล้วในช่องกรอก (ยังไม่กดสมัคร) → ขึ้นข้อความเตือนเล็กๆ สีแดงใต้ช่องทันทีหลังพิมพ์เสร็จ (ไม่ต้องกดปุ่มสมัครก่อน)
- [x] พิมพ์ username ที่ยังไม่มีในระบบ → ไม่ขึ้นข้อความเตือน
- [x] พิมพ์ username ซ้ำแล้วลบออก/แก้เป็นชื่ออื่น → ข้อความเตือนหายไปถูกต้อง ไม่ค้าง
- [x] สลับไปโหมด login แล้วกลับมาโหมดสมัครใหม่ → ข้อความเตือนไม่ค้างมาจากรอบก่อนหน้า
- [x] กดสมัครรัวๆ เกิน 5 ครั้งภายใน 60 วินาที → ครั้งที่เกินโดนบล็อกด้วยข้อความ rate limit
- [x] ไม่กรอกชื่อ-นามสกุล → ถูกปฏิเสธทั้งฝั่งหน้าเว็บและ backend (ทดสอบ backend โดยตรงได้ผ่าน Postman/console ถ้าจะเช็คให้ครบ)
- [x] ไม่กรอกแผนก/ทีม → ถูกปฏิเสธทั้งฝั่งหน้าเว็บและ backend
- [x] หลังสมัครสำเร็จ เข้าไปดูชีต `Users` → มีแถวใหม่ครบ username/salt/hash/role/created_at/email/full_name/department ถูกต้อง

**หมายเหตุ:**

## 2. เข้าสู่ระบบ (Login) + Lockout

- [x] Login ด้วย username/password ถูกต้อง → เข้าสู่ Dashboard สำเร็จ
- [x] Login ด้วยรหัสผ่านผิด → ขึ้น error, มีบันทึกใน `ActivityLogs` (LOGIN_FAIL)
- [x] กรอกรหัสผิดครบ 3 ครั้งติดกัน → ปุ่ม "เข้าสู่ระบบ" เปลี่ยนเป็นสีเทา กดไม่ได้ พร้อมนับถอยหลัง 60 วินาที
- [x] รอครบเวลาแล้วลอง login ใหม่ด้วยรหัสถูกต้อง → เข้าได้ปกติ
- [x] บัญชีที่มีอีเมลผูกไว้ โดนล็อกแล้วมีอีเมลแจ้งเตือนความปลอดภัยส่งไปจริง
- [x] ทำให้โดนล็อกซ้ำภายใน 1 ชั่วโมง (username เดิม) → ไม่ส่งอีเมลซ้ำ (rate limit 1 ฉบับ/ชั่วโมง)
- [x] Login ด้วย username ที่ไม่มีในระบบ → ขึ้น error แบบเดียวกับรหัสผิด (ไม่บอกว่าไม่มี username นี้)
- [x] Login สำเร็จแล้วรีเฟรชหน้า → ยังอยู่ในระบบ (ไม่ต้อง login ใหม่)
- [x] ไม่มีการใช้งานเกิน 10 นาที → ระบบ auto logout อัตโนมัติ
- [x] ใช้งานต่อเนื่อง (ขยับเมาส์/พิมพ์/คลิก) → session ต่ออายุไปเรื่อยๆ ไม่หลุด

**หมายเหตุ:**

## 3. ลืมรหัสผ่าน (OTP ทางอีเมล)

- [x] กด "ลืมรหัสผ่าน" กรอก username ที่มีอีเมลผูกไว้ → ได้รับอีเมล OTP 6 หลัก
- [x] กรอก username ที่ยังไม่มีอีเมลผูกไว้ → ขึ้นข้อความแจ้งให้ติดต่อ Super_Admin ตั้งอีเมลก่อน
- [x] กรอก username ที่ไม่มีในระบบ → ไม่เปิดเผยว่าไม่มี username นี้จริง (ข้อความทั่วไป) แต่ยังถูก rate limit เหมือนกัน
- [x] กรอกรหัส OTP ถูกต้อง → ผ่านไปขั้นตอนตั้งรหัสผ่านใหม่
- [x] กรอกรหัส OTP ผิด → ขึ้น error, นับจำนวนครั้งที่ผิด
- [x] กรอกรหัส OTP ผิดครบ 5 ครั้ง → OTP ใช้ไม่ได้อีก ต้องขอรหัสใหม่
- [x] รอเกิน 5 นาทีแล้วกรอก OTP เดิม → หมดอายุ ใช้ไม่ได้
- [x] กด "ขอรหัสใหม่" ที่หน้ากรอก OTP โดยตรง (ไม่ต้องย้อนกลับไปหน้าลืมรหัสผ่าน) → ได้ OTP ใหม่ ปุ่มขึ้นข้อความถูกต้องไม่ค้าง
- [x] ขอ OTP เกิน 3 ครั้งภายใน 10 นาที (username เดียวกัน) → ถูกบล็อกไม่ให้ขอต่อ 10 นาที
- [x] ตั้งรหัสผ่านใหม่สำเร็จหลังยืนยัน OTP → login ด้วยรหัสใหม่ได้ทันที และปลดล็อก lockout ที่อาจติดค้างอยู่ให้อัตโนมัติ
- [x] รหัสผ่านใหม่ที่ตั้งไม่ตรงเงื่อนไขความปลอดภัย → ถูกปฏิเสธเหมือนตอนสมัคร
- [x] ยืนยัน OTP สำเร็จแล้วตั้งรหัสผ่านใหม่ให้เหมือนรหัสผ่านเดิมเป๊ะ → ถูกปฏิเสธพร้อมข้อความ "รหัสผ่านใหม่ต้องไม่ซ้ำกับรหัสผ่านเดิม" (เหมือนหมวด 4)

**หมายเหตุ:**

## 4. เปลี่ยนรหัสผ่านตัวเอง (ขณะ login อยู่)

- [x] กรอกรหัสผ่านปัจจุบันถูกต้อง + รหัสใหม่ตรงเงื่อนไข → เปลี่ยนสำเร็จ
- [x] กรอกรหัสผ่านปัจจุบันผิด → ถูกปฏิเสธ ไม่เปลี่ยน
- [x] รหัสใหม่ไม่ตรงเงื่อนไข → ถูกปฏิเสธพร้อมบอกเงื่อนไข
- [x] กรอกรหัสผ่านใหม่ให้เหมือนรหัสผ่านปัจจุบันเป๊ะ → ถูกปฏิเสธพร้อมข้อความ "รหัสผ่านใหม่ต้องไม่ซ้ำกับรหัสผ่านเดิม"

**หมายเหตุ:**

## 5. สร้างข้อมูลทดสอบ (Generate)

- [x] สร้างข้อมูลครบทุก dialect ที่ระบบรองรับ อย่างน้อยคนละ 1 ครั้ง (PostgreSQL, MySQL, MS SQL, Oracle, MongoDB ✓ ครบ)
- [x] สร้างโดยแนบ DDL script → ข้อมูลที่ได้ตรงตามโครงสร้างที่กำหนด
- [x] สร้างโดยไม่แนบ DDL (พิมพ์ prompt เอง) → ทำงานได้ปกติ
- [x] สร้างโดยแนบรูป Schema/ER Diagram → ข้อมูลที่ได้สอดคล้องกับรูป
- [x] เปิด/ปิดตัวเลือก "อนุญาตค่าว่าง (allow null)" → ผลลัพธ์เปลี่ยนตามจริง (ปิด=ไม่มีว่างเลย ครั้งที่ 1, เปิด=มี null จริง 2 แถว ครั้งที่ 2)
- [x] กดปุ่ม "สร้างข้อมูลทดสอบ" รัวๆ หลายครั้งติดกัน → ยิง Gemini แค่ 1 ครั้ง ไม่ซ้อนกัน (ปุ่มถูก disable ระหว่างรอผล)
- [x] คะแนนคุณภาพ (% ตรงเงื่อนไข / % ความน่าเชื่อถือ) แสดงผลสมเหตุสมผลและถูกบันทึกลง `QualityScores`
- [x] แถบโควตา Gemini ลดลง 2 ขีดทุกครั้งที่กด generate 1 ครั้ง
- [x] ใช้จนแถบโควตาขึ้นเหลือง/แดง → มีคำเตือนแสดงถูกต้อง (ตรวจจากโค้ดแทนการเทสจริงเพื่อประหยัด Token: `renderGeminiQuotaFromTimestamps` ที่ 60-89% ขึ้นเหลือง+คำเตือน, ≥90% ขึ้นแดง+คำเตือน ⚠️, <60% ซ่อนคำเตือน — logic/element ครบถูกต้อง)
- [x] ช่องค้นหา "ค้นหาข้อมูลจำลอง" กรองผลลัพธ์แบบ real-time ถูกต้อง
- [x] สร้างข้อมูลที่ตรงตามเงื่อนไข <70% หรือ ความน่าเชื่อถือ <90% → ปุ่ม "ยืนยันนำเข้าสู่ Dashboard" ที่หน้าจุดพักถูกบล็อก (สีเทา กดไม่ได้) พร้อมข้อความอธิบาย (ตรวจจากโค้ดแทนการเทสจริง เพราะบังคับให้คะแนนต่ำจริงยาก — logic ตรงตามที่ตกลงกันครบ: บล็อกทันทีที่ conditionMatchPercent<70 หรือ reliabilityPercent<90 ด้วย disabled attribute จริง ไม่ใช่แค่เปลี่ยนสี)
- [x] สร้างข้อมูลที่ผ่านเกณฑ์ขั้นต่ำ (≥70%/≥90%) → กดนำเข้าได้ปกติ พร้อมป้าย "ระดับข้อมูลที่สร้างได้" (พื้นฐาน/ปานกลาง/ขั้นสูง) ตรงกับคะแนนจริง แสดงทั้งหน้าจุดพักและใต้ตาราง Dashboard
- [x] กรอกจำนวนแถวเกิน 300 → ถูกปฏิเสธทั้งฝั่งหน้าเว็บ (alert ก่อนยิง request) และฝั่ง backend (ถ้ายิงตรงข้าม request ก็ต้องถูกปฏิเสธเหมือนกัน) พร้อมข้อความอธิบายเรื่องข้อจำกัดความยาวคำตอบของโมเดล — เปลี่ยนจากเพดานเดิม 1,000 เหลือ 300 ใน v58 — ตรวจจากโค้ด: `Final.html` เช็ค `rowsCount > 300` ก่อนยิง request จริง, `Code.gs` เช็ค `rowsRequested > MAX_ROWS_PER_GENERATE` (=300) ซ้ำอีกชั้นพร้อมข้อความอธิบายครบ ยังไม่ได้ทดสอบจริง

**หมายเหตุ:** ครั้งที่ 1 (PostgreSQL, มี DDL, ปิด allow null), ครั้งที่ 2 (MySQL, เปิด allow null), ครั้งที่ 3 (MS SQL, มี DDL, ปิด allow null), ครั้งที่ 4 (Oracle, เปิด allow null) ผ่านหมด — ครั้งที่ 5 รอบแรก (MongoDB + แนบรูป ER) พบบั๊กจริง: แนบรูปแล้วแต่ Gemini เมินรูปไปเลย ได้ข้อมูล access_logs ที่ไม่เกี่ยวกับรูป แก้ไขแล้วที่ `buildSmartPrompt_` (v52) — **ทดสอบซ้ำแล้วผ่าน**: แนบรูป ER (CUSTOMERS↔ORDERS 1:N) ไม่บอกชื่อคอลัมน์ในข้อความเลย ได้ตาราง orders กลับมาตรงตามรูปเป๊ะ (order_id PK, customer_id FK อ้างอิง CUSTOMERS, product_name, quantity, order_date) และ customer_id ซ้ำกันหลายแถวสะท้อนความสัมพันธ์ 1:N จริง คุณภาพ 100%/100% ระดับขั้นสูง — บั๊กแก้สำเร็จแล้ว

## 6. ส่งออกข้อมูล (Export)

- [x] ดาวน์โหลด .sql → syntax ตรงกับ dialect ที่เลือก เปิดไฟล์ได้ปกติ
- [x] ดาวน์โหลด .json → โครงสร้างถูกต้อง ไม่มีฟิลด์ภายใน (เช่น `is_edited`) หลุดออกมา
- [x] แก้ไขค่าบางเซลล์ในตารางผลลัพธ์ก่อน export → ค่าที่แก้ไขสะท้อนในไฟล์ที่ export ถูกต้อง

**หมายเหตุ:** ตรวจ .sql/.json จากทั้ง 5 dialect ที่ generate มาแล้ว — syntax ถูกต้องตาม dialect ทุกไฟล์ (Postgres ใช้ "double quote", MySQL ใช้ `backtick`, MS SQL ใช้ [bracket], Oracle ใช้ "double quote"+TO_DATE(), MongoDB ใช้ db.collection.insertMany()) และ .json ทุกไฟล์ไม่มีฟิลด์แปลกปลอมอย่าง `is_edited` หลุดออกมาเลย — ข้อ "แก้ไขเซลล์ก่อน export" ตรวจโค้ดแล้วว่า input event เขียนค่ากลับเข้า row object เดียวกับที่ export ใช้จริง ผ่านครบทั้งหมวดแล้ว

## 7. บันทึกลงฐานข้อมูล (Commit)

- [x] กรอกรหัสผ่านยืนยันถูกต้อง → commit สำเร็จ
- [x] กรอกรหัสผ่านยืนยันผิด → ถูกปฏิเสธ ไม่บันทึก
- [x] เข้าไปดูชีต `GeneratedDatasets` → ข้อมูลถูกจัดกลุ่มใต้ป้ายชื่อรอบ Commit ถูกต้อง
- [x] Commit ซ้ำหลายรอบในวันเดียวกัน → แต่ละรอบแยกกลุ่มชัดเจน ไม่ปนกัน

**หมายเหตุ:**

## 8. บันทึก Prompt ไว้ใช้ซ้ำ (Saved Prompts)

- [x] บันทึก prompt ใหม่พร้อมตั้งชื่อ → ปรากฏในรายการที่บันทึกไว้
- [x] บันทึก prompt พร้อมแนบรูป → รูปถูกเก็บใน Drive และดึงกลับมาแสดงได้ถูกต้อง
- [x] กด "โหลดมาใช้" กับ prompt ที่เคยบันทึก → ฟอร์มถูกเติมค่าครบ รวมรูปที่แนบไว้
- [x] กดลบ prompt ที่บันทึกไว้ → หายจากรายการและหายจากชีต `SavedPrompts` จริง

**หมายเหตุ:**

## 9. ประวัติชุดข้อมูลที่เคย Commit

- [x] เปิดหน้าประวัติ → เห็นรายการวันที่ที่เคย commit
- [x] เลือกวันที่ → เห็นรอบ Commit ของวันนั้นครบ
- [x] เลือกรอบ Commit → เห็นตัวอย่างตาราง + ปุ่มดาวน์โหลด .sql/.csv/.json ใช้งานได้ครบ
- [x] กดลบรอบ Commit → หายจากรายการ และหายทั้งบล็อก (ป้ายชื่อ+หัวตาราง+ข้อมูล) จากชีต `GeneratedDatasets` จริง

**หมายเหตุ:**

## 10. รายงานคุณภาพข้อมูล (Quality Report)

- [x] เปิดรายงาน → ข้อมูลจัดกลุ่มตามวันที่ถูกต้อง ตรงกับที่ generate ไปจริง

**หมายเหตุ:**

## 11. แผงจัดการผู้ใช้งาน (Super_Admin เท่านั้น)

- [x] Login ด้วย role อื่นที่ไม่ใช่ Super_Admin → ไม่เห็นปุ่ม "จัดการผู้ใช้งาน"
- [x] Login ด้วย Super_Admin → เห็นปุ่มและเปิดแผงได้ พร้อมคอลัมน์อีเมล/ชื่อ-นามสกุล/แผนกครบ ตรงกับที่ผู้ใช้แต่ละคนกรอกไว้ตอนสมัคร
- [x] รีเซ็ตรหัสผ่านผู้ใช้อื่น → สำเร็จ ตรวจเงื่อนไขรหัสผ่านเหมือนตอนสมัคร
- [x] ตั้ง/แก้ไขอีเมลผู้ใช้อื่นเป็นโดเมนอื่นที่ไม่ใช่ gmail → ทำได้ (ไม่ถูกจำกัดแบบตอนสมัคร)
- [x] ลบผู้ใช้อื่น → สำเร็จ หายจากชีต `Users`
- [x] พยายามลบบัญชีตัวเองขณะ login อยู่ → ถูกบล็อก
- [x] พยายามลบ Super_Admin คนสุดท้ายที่เหลืออยู่ → ถูกบล็อก
- [x] Login ด้วยผู้ใช้ทั่วไป (ไม่ใช่ Admin123) → เห็นปุ่ม "🗑️ ขอลบบัญชี" ที่หัวหน้า Dashboard เสมอ ไม่ว่า role ไหนก็ตาม (ยกเว้น Admin123 ดูรายการ v60 ด้านล่าง) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] กด "ขอลบบัญชี" → ขึ้น modal ยืนยันก่อน กดยืนยันแล้วขึ้นข้อความสำเร็จ ไม่ได้ล็อกเอาต์หรือลบบัญชีทันที (ยัง login ใช้งานต่อได้ปกติ) — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] หลังส่งคำขอ → มีแถวใหม่ใน `ActivityLogs` action `REQUEST_DELETE_ACCOUNT` และ Super_Admin ทุกคนที่มีอีเมลผูกไว้ได้รับอีเมลแจ้งเตือนจริง — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] กด "ขอลบบัญชี" ซ้ำภายใน 24 ชม. เดิม → ถูกปฏิเสธพร้อมข้อความอธิบายว่าเพิ่งส่งคำขอไปแล้ว (ตรวจจากโค้ด: cache key `DELETE_ACCOUNT_REQUEST_` + username คงอยู่ 86400 วินาที) — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] Super_Admin เห็นคำขอนี้ผ่านอีเมล → เข้าแผงจัดการผู้ใช้งานแล้วกดลบบัญชีนั้นได้ตามขั้นตอนเดิมทุกอย่าง (`handleDeleteUser_` ไม่ถูกแตะ) — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้ (หมายเหตุ: ตอนทดสอบยังเป็นสถานะก่อน v59 ที่มีแค่อีเมล ไม่มี badge ในแผง ดูรายการทดสอบเพิ่มเติมของ v59 ด้านล่าง)
- [x] สร้างรายงาน PDF ใหม่ → หัวข้อ "1. สรุปความปลอดภัยระบบ" มีแถว "ผู้ใช้ขอให้ลบบัญชีตัวเอง (REQUEST_DELETE_ACCOUNT)" นับจำนวนคำขอที่ส่งไปตรงกับที่ทดสอบจริง — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] **(v59)** Super_Admin เปิดแผงจัดการผู้ใช้งานหลังมีคำขอค้างอยู่ → เห็นแบนเนอร์สีแดง "⚠️ มี N คำขอขอลบบัญชีที่รอดำเนินการ" ด้านบนตาราง และแถวของผู้ใช้ที่ขอไว้ถูกไฮไลต์สีแดงอ่อน พร้อมป้าย "🗑️ ขอลบบัญชีเมื่อ..." ใต้ username — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] **(v59)** ผู้ใช้ที่มีคำขอค้างถูกเรียงขึ้นแถวบนสุดของตารางเสมอ (เทียบกับผู้ใช้ที่ไม่มีคำขอค้าง) — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] **(v59)** แถวที่มีคำขอค้างมีปุ่ม "ยกเลิกคำขอ" เพิ่มขึ้นมา (แถวปกติไม่มีปุ่มนี้) กดแล้วขึ้น modal ยืนยันสีเหลือง (ไม่ใช่สีแดงแบบลบจริง) เพื่อไม่ให้สับสนกับปุ่ม "ลบ" — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] **(v59)** กดยืนยัน "ยกเลิกคำขอ" → คำขอหายไป (คอลัมน์ `delete_requested_at` ถูกล้าง), badge/ไฮไลต์หายจากแถวนั้น, แบนเนอร์นับจำนวนลดลงหรือหายไปถ้าเหลือ 0, มีแถวใหม่ใน `ActivityLogs` action `DISMISS_DELETE_ACCOUNT_REQUEST`, บัญชีผู้ใช้ยังอยู่ครบไม่ถูกลบ — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] **(v59)** หลังถูกยกเลิกคำขอแล้ว เจ้าของบัญชีกด "ขอลบบัญชี" ใหม่ได้ทันที ไม่ต้องรอครบ 24 ชม.จากคำขอเดิม (cooldown cache ถูกล้างพร้อมกับตอนยกเลิก) — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] **(v59)** Super_Admin กดปุ่ม "ลบ" ตรงๆ ที่แถวมีคำขอค้าง (ไม่ผ่านปุ่มยกเลิกก่อน) → ลบบัญชีได้สำเร็จตามปกติเหมือนแถวทั่วไปทุกประการ (ยืนยันว่า flow ลบเดิมไม่ถูกกระทบจากการเพิ่มฟีเจอร์นี้) — ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้
- [x] **(v60)** Login ด้วย Admin123 → ไม่เห็นปุ่ม "🗑️ ขอลบบัญชี" ที่หัวหน้า Dashboard เลย (ต่างจากผู้ใช้อื่นทุกคนรวมถึง Super_Admin คนอื่น) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v60)** ยิง action `requestDeleteAccount` ตรงๆ ด้วย username `Admin123` (ข้ามการซ่อนปุ่มฝั่งหน้าเว็บ เช่นผ่าน DevTools) → backend ปฏิเสธเช่นกัน พร้อมข้อความอธิบายว่าบัญชีนี้ต้องมีอยู่เสมอ (ยืนยันว่าไม่ได้พึ่งการซ่อนปุ่มฝั่งหน้าเว็บอย่างเดียว) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v60)** สมัครสมาชิกใหม่เลือก role "Super Admin" → เห็นข้อความหมายเหตุใต้ dropdown ว่าต้องรออนุมัติ กดสมัครแล้วขึ้นข้อความ "⏳ รอการอนุมัติจากระบบ" (ไม่ใช่ข้อความสำเร็จปกติแบบ role อื่น) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v60)** หลังสมัคร Super Admin ใหม่ → Admin123 ได้รับอีเมลแจ้งคำขอ (ผู้สมัครไม่ได้รับ ยังไม่มีสิทธิ์อะไร) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v60)** ผู้สมัคร Super Admin ที่ยังไม่ได้รับอนุมัติพยายาม Login (รหัสผ่านถูกต้อง) → ถูกบล็อกพร้อมข้อความอธิบายว่ารออนุมัติอยู่ ไม่นับเป็นการ login ผิดพลาด (ไม่กระทบ login lockout) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v60)** Admin123 เปิดแผงจัดการผู้ใช้งาน → เห็นแบนเนอร์สีม่วง "⏳ มี N คำขอสมัคร Super Admin ที่รอการอนุมัติ" แถวของผู้สมัครไฮไลต์สีม่วง พร้อมป้าย "⏳ รอการอนุมัติ Super Admin" และปุ่ม "✅ อนุมัติ"/"❌ ปฏิเสธ" — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v60)** Super_Admin คนอื่น (ไม่ใช่ Admin123) เปิดแผงจัดการผู้ใช้งาน → เห็นแถวผู้สมัครที่รออนุมัติพร้อมป้ายสถานะเหมือนกัน แต่**ไม่เห็น**ปุ่ม "✅ อนุมัติ"/"❌ ปฏิเสธ" และแบนเนอร์สรุปคำขอ — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v60)** Admin123 กด "✅ อนุมัติ" → ผู้สมัคร Login ได้ทันที ได้รับอีเมลแจ้งผลอนุมัติ แถว/badge/ปุ่มอนุมัติหายไปจากตาราง มีแถวใหม่ใน `ActivityLogs` action `APPROVE_SUPER_ADMIN_REQUEST` — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v60)** Admin123 กด "❌ ปฏิเสธ" กับผู้สมัครอีกคน → บัญชีถูกลบออกจากชีต `Users` ทันที ผู้สมัครได้รับอีเมลแจ้งผลปฏิเสธ มีแถวใหม่ใน `ActivityLogs` action `REJECT_SUPER_ADMIN_REQUEST` — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v60)** Super_Admin คนอื่น (ไม่ใช่ Admin123) ยิง action `approveSuperAdminRequest`/`rejectSuperAdminRequest` ตรงๆ (ข้าม UI) → backend ปฏิเสธเช่นกัน (ยืนยันว่าไม่ได้พึ่งการซ่อนปุ่มฝั่งหน้าเว็บอย่างเดียว) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v61)** Super_Admin คนอื่น (ไม่ใช่ Admin123, ระบบมี Super_Admin ≥2 คน) เปิดแผงจัดการผู้ใช้งาน → ปุ่ม "ลบ" ที่แถวของ Admin123 เป็นสีเทากดไม่ได้ (disabled) พร้อม tooltip อธิบายว่าเป็น Super Admin หลัก — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v61)** ยิง action `deleteUser` ตรงๆ ด้วย `targetUsername: "Admin123"` (ข้ามการปิดปุ่มฝั่งหน้าเว็บ เช่นผ่าน DevTools) โดย requester เป็น Super_Admin คนอื่น → backend ปฏิเสธเช่นกัน พร้อมข้อความอธิบายว่าต้องมีอยู่เสมอ (ยืนยันว่าไม่ได้พึ่งการปิดปุ่มฝั่งหน้าเว็บอย่างเดียว) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v61)** ลบผู้ใช้อื่นที่ไม่ใช่ Admin123 (role อะไรก็ได้ รวมถึง Super_Admin คนอื่นที่ไม่ใช่คนสุดท้าย) → ยังลบได้ตามปกติทุกประการ (ยืนยันว่าการแก้ครั้งนี้จำกัดเฉพาะ Admin123 เท่านั้น ไม่กระทบ flow ลบผู้ใช้อื่น) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v62)** Admin123 เปิดแผงจัดการผู้ใช้งานที่มีคำขอสมัคร Super_Admin รออนุมัติ → แถวนั้นเห็นแค่ปุ่ม "ตั้ง/แก้ไขอีเมล", "รีเซ็ตรหัสผ่าน", "✅ อนุมัติ", "❌ ปฏิเสธ" **ไม่มี**ปุ่ม "ลบ" ธรรมดาปนอยู่เลย — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v62)** Super_Admin คนอื่น (ไม่ใช่ Admin123) เปิดแผงจัดการผู้ใช้งานที่มีคำขอสมัคร Super_Admin รออนุมัติ → แถวนั้นเห็นป้ายสถานะ "⏳ รอการอนุมัติ Super Admin" เฉยๆ ไม่มีปุ่ม "ลบ" และไม่มีปุ่ม "✅ อนุมัติ"/"❌ ปฏิเสธ" เลย (ไม่มีทางลบบัญชีนี้ได้จากหน้าเว็บเลยนอกจาก Admin123 กดปฏิเสธ) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v62)** ยิง action `deleteUser` ตรงๆ ด้วย `targetUsername` ของบัญชีที่ `approvalStatus==='pending'` (ข้ามการซ่อนปุ่มฝั่งหน้าเว็บ เช่นผ่าน DevTools) → backend ปฏิเสธ พร้อมข้อความแนะนำให้ใช้ปุ่ม "❌ ปฏิเสธ" แทน (ยืนยันว่าไม่ได้พึ่งการซ่อนปุ่มฝั่งหน้าเว็บอย่างเดียว) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v62)** Admin123 กด "❌ ปฏิเสธ" → ผู้สมัครได้รับอีเมลที่ระบุชัดเจนว่า username/password ที่สมัครไว้ถูกลบแล้ว และเชิญให้สมัครสมาชิกใหม่ได้ทันที (ทดสอบว่าใช้ username เดิมสมัครซ้ำได้จริงหลังถูกปฏิเสธ) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v63)** สมัคร user ใหม่ (role อะไรก็ได้) → กด "ขอลบบัญชี" ให้สำเร็จ 1 ครั้ง → ให้ Super_Admin กดปุ่ม "ลบ" ลบบัญชีนี้ทิ้งผ่านแผงจัดการผู้ใช้งาน → สมัคร username เดิมซ้ำทันที (ภายใน 24 ชม.) → login เข้าบัญชีใหม่แล้วกด "ขอลบบัญชี" → ต้องส่งคำขอสำเร็จทันที **ไม่ขึ้น error** "เพิ่งส่งคำขอลบบัญชีไปแล้ว" (ยืนยันว่า cache ของบัญชีเก่าถูกล้างไปพร้อมกับตอนลบแล้ว) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v63)** ทำซ้ำเคสเดียวกันแต่ใช้ path "สมัคร Super_Admin → Admin123 กดปฏิเสธ → สมัคร username เดิมซ้ำ" แทน path "ลบผ่านปุ่มลบธรรมดา" → ผลลัพธ์ต้องเหมือนกัน คือกด "ขอลบบัญชี" ของบัญชีใหม่ได้ทันทีไม่ติด cache เก่า — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v63)** สมัคร username เดิมซ้ำหลังบัญชีเก่าถูกลบ (path ไหนก็ได้) → login ด้วยรหัสผ่านใหม่ที่เพิ่งตั้งได้ปกติ ไม่ติด login lockout ค้างจากบัญชีเก่า (ถ้าบัญชีเก่าเคยพิมพ์รหัสผิดจนเกือบล็อกไว้ก่อนถูกลบ) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v64)** ผู้ใช้ส่งคำขอ "ขอลบบัญชี" → Super_Admin (คนไหนก็ได้ ไม่จำกัดแค่ Admin123) กดปุ่ม "ลบ" ทำตามคำขอ → เจ้าของบัญชีได้รับอีเมลแจ้งว่าคำขอได้รับอนุมัติแล้วและบัญชี/username/password ถูกลบไปแล้ว — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v64)** ผู้ใช้ส่งคำขอ "ขอลบบัญชี" → Super_Admin กดปุ่ม "ยกเลิกคำขอ" (ไม่ลบ) → เจ้าของบัญชีได้รับอีเมลแจ้งว่าคำขอถูกยกเลิก บัญชียังอยู่และใช้งานได้ตามปกติ — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v64)** Super_Admin ลบผู้ใช้ทั่วไปที่ไม่เคยส่งคำขอ "ขอลบบัญชี" มาก่อน (ไม่มี `delete_requested_at`) → ลบสำเร็จตามปกติ **ไม่มี**อีเมลแจ้งผลส่งออกไป (ยืนยันว่าอีเมลนี้ส่งเฉพาะกรณีลบตามคำขอของเจ้าของบัญชีเองเท่านั้น) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v64)** ผู้ที่ส่งคำขอ "ขอลบบัญชี" ไม่มีอีเมลผูกไว้ในระบบ (บัญชีเก่าก่อนมีฟีเจอร์อีเมล) → Super_Admin ยังกดปุ่ม "ลบ"/"ยกเลิกคำขอ" ได้ตามปกติ ไม่มี error ใดๆ เกิดขึ้น (แค่ข้ามการส่งอีเมลไปเงียบๆ เพราะไม่มีอีเมลให้ส่ง) — ตรวจจากโค้ด: อ่านโค้ดจริงใน Code.gs/Final.html ยืนยันตรงตามพฤติกรรมที่ระบุ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ
- [x] **(v65)** พยายามยิง action `deleteUser`/`dismissDeleteAccountRequest`/`getAllUsers`/`adminResetPassword`/`adminSetUserEmail` ตรงๆ โดยใส่ `requestingUsername` เป็นชื่อบัญชีที่สมัคร role "Super Admin" ไว้แต่ยังไม่ได้รับอนุมัติ (`approval_status === 'pending'`, login ปกติเข้าไม่ได้) → backend ต้องปฏิเสธทุก action เช่นเดียวกับ username ที่ไม่ใช่ Super_Admin เลย (ยืนยันว่าคำขอสมัครที่ยังรออนุมัติ ไม่สามารถใช้สิทธิ์ Super_Admin ทำอะไรได้เลยแม้จะยิง request ตรงๆ ข้าม UI ก็ตาม) — ตรวจจากโค้ด: `isSuperAdmin_()` เพิ่มเงื่อนไข `approvalStatus !== 'pending'` แล้ว ยังไม่ได้ทดสอบจริง
- [x] **(v65)** กด "ตรวจสอบความพร้อมก่อน Deploy" ตอนที่ Super_Admin ตัวเดียวในระบบเป็นบัญชีที่ยังรออนุมัติอยู่ (ไม่มี Super_Admin ที่อนุมัติแล้วเลยสักคน) → ต้องขึ้น "❌ ยังไม่มีผู้ใช้ role Super_Admin เลย" ไม่ใช่ "✅" (ยืนยันว่าไม่นับคำขอที่รออนุมัติเป็น Super_Admin ที่ใช้งานได้จริง) — ตรวจจากโค้ด: `checkDeployReadiness()` เพิ่มเงื่อนไข `approval_status !== 'pending'` แล้ว ยังไม่ได้ทดสอบจริง

**หมายเหตุ:**

- รายการที่มีคำว่า "ผู้ใช้ทดสอบจริงยืนยันตรงตามนี้" (รวมทั้ง 4 ข้อเดิมของ v58 และ 6 ข้อของ v59) ถูกติ๊กจากผลทดสอบจริงที่ผู้ใช้รายงานกลับมาโดยตรง (ไม่ใช่ code review) — สอดคล้องกับโค้ดที่ตรวจสอบไว้ก่อนหน้านี้แล้วเช่นกัน
- รายการ **(v59)** ทั้งหมดผ่านการทดสอบจริงแล้วครบ (ดูด้านบน)
- รายการที่ขึ้นต้นด้วย **(v60)** เป็นรายการใหม่ที่ต้องทดสอบหลัง deploy โค้ด v60 (ล็อกสิทธิ์ Super Admin หลัก + บังคับอนุมัติก่อนสมัคร Super_Admin ใหม่) ยังไม่เคยทดสอบจริง
- รายการที่ขึ้นต้นด้วย **(v61)** เป็นรายการใหม่ที่ต้องทดสอบหลัง deploy โค้ด v61 (ปิดช่องโหว่ Super_Admin คนอื่นยังลบ Admin123 ได้ผ่านปุ่ม "ลบ" ปกติ) ยังไม่เคยทดสอบจริง
- รายการที่ขึ้นต้นด้วย **(v62)** เป็นรายการใหม่ที่ต้องทดสอบหลัง deploy โค้ด v62 (แถวคำขอสมัคร Super_Admin ไม่มีปุ่ม "ลบ" ธรรมดาอีกต่อไป ต้องผ่านปุ่ม "ปฏิเสธ" เท่านั้น) ยังไม่เคยทดสอบจริง
- รายการที่ขึ้นต้นด้วย **(v63)** เป็นรายการใหม่ที่ต้องทดสอบหลัง deploy โค้ด v63 (แก้บั๊ก cache เก่าของบัญชีที่ถูกลบเล่นงานบัญชีใหม่ที่สมัคร username ซ้ำ) — บั๊กนี้ผู้ใช้เจอเองจริงระหว่างทดสอบ v62 (ทุก role ไม่ใช่แค่ Super_Admin) ยังไม่เคยทดสอบว่าแก้แล้วหายจริง
- รายการที่ขึ้นต้นด้วย **(v64)** เป็นรายการใหม่ที่ต้องทดสอบหลัง deploy โค้ด v64 (ส่งอีเมลแจ้งผลคำขอ "ขอลบบัญชี" ทั้งกรณีลบจริงและยกเลิกคำขอ เปิดให้ Super_Admin ทุกคนทำได้เหมือนเดิม ไม่ได้จำกัดแค่ Admin123) ยังไม่เคยทดสอบจริง
- รายการ **(v60)-(v64)** ทั้งหมด (25 ข้อ) ติ๊กผ่านจากการทำ code review รอบใหม่ (recheck ทั้งระบบตามคำขอผู้ใช้ วันที่ 21/07/2026) — อ่านโค้ดจริงเทียบกับพฤติกรรมที่ระบุไว้ทีละข้อจนครบ ยืนยันว่าตรงตามที่ออกแบบไว้ทุกข้อ **แต่ยังไม่มีข้อไหนถูกทดสอบจริงผ่านหน้าเว็บเลย** — ระหว่าง code review รอบนี้พบและแก้บั๊กจริง 3 จุด (ไม่เกี่ยวกับรายการข้างบนที่ติ๊กไปแล้วโดยตรง แต่กระทบความถูกต้อง/ความปลอดภัยของฟีเจอร์เดียวกัน): (1) อีเมลใน `handleDismissDeleteAccountRequest_` มี markdown `**...**` หลงเหลืออยู่ทั้งที่อีเมลเป็น plain text ทั้งระบบ — แก้แล้ว (2) `getSuperAdminEmails_()` และตัวนับ `superAdminCount` ใน `handleDeleteUser_`/`collectSecuritySummary_` นับคำขอสมัคร Super_Admin ที่ยังรออนุมัติ (`approval_status==='pending'`) รวมเป็น Super_Admin ที่ใช้งานได้จริงด้วย ทำให้ผู้สมัครที่ login ไม่ได้เลยได้รับอีเมลแจ้งคำขอลบบัญชีของคนอื่น และตัวเลขในรายงาน PDF/การป้องกันลบ Super_Admin คนสุดท้ายคลาดเคลื่อน — แก้แล้ว (3) **(v65, ช่องโหว่ด้านสิทธิ์)** `isSuperAdmin_()` และ `checkDeployReadiness()` เดิมก็นับคำขอ Super_Admin ที่รออนุมัติเป็น Super_Admin ที่ใช้งานได้เช่นกัน ทำให้ผู้สมัครที่ยังไม่ได้รับอนุมัติ (login ปกติเข้าไม่ได้) สามารถยิง action สงวนสิทธิ์ (`deleteUser`, `getAllUsers`, `adminResetPassword`, `adminSetUserEmail`, `dismissDeleteAccountRequest`) ตรงๆ ผ่าน `requestingUsername` ของตัวเองได้โดยไม่ผ่านการอนุมัติเลย — แก้แล้วโดยเพิ่มเงื่อนไข `approval_status !== 'pending'` ในทั้ง 2 ฟังก์ชัน ดูรายละเอียดเพิ่มเติมใน `IMPROVEMENT_CHECKLIST.md` และ `DEPLOY_GUIDE.md`

## 12. Dark Mode / Responsive

- [x] สลับ Dark Mode / Light Mode → ใช้งานได้ครบทุกหน้า ไม่มีจุดที่สีเพี้ยน/อ่านไม่ออก
- [x] รีเฟรชหน้า → ธีมที่เลือกไว้ยังคงอยู่
- [x] เปิดผ่านมือถือ/หน้าจอแคบ → แถบปุ่มหัว Dashboard ไม่ล้นจอ (wrap ได้ปกติ) — ตรวจจากโค้ด: `<div class="flex items-center gap-3 flex-wrap justify-start md:justify-end">` ครอบปุ่มหัว Dashboard ทุกปุ่มไว้แล้ว ยังไม่ได้ทดสอบจริงบนอุปกรณ์
- [x] เปิดหน้าต่างยืนยัน Commit บนมือถือ → ช่องกรอกรหัสผ่าน+ปุ่มไม่ล้นจอ — ตรวจจากโค้ด: แถวยืนยัน Commit ใช้ `flex flex-wrap` + `w-full sm:w-auto` ทั้งช่องกรอกและปุ่มแล้ว ยังไม่ได้ทดสอบจริงบนอุปกรณ์
- [ ] ทดสอบ flow หลัก (login, generate, commit) บนมือถือจริงอย่างน้อย 1 รอบ — ข้อนี้เป็นการทดสอบ flow จริงบนอุปกรณ์มือถือจริง ไม่ใช่พฤติกรรมเฉพาะจุดที่ตรวจจากโค้ดได้ตรงๆ (ต้องลองเปิดเว็บบนมือถือจริงเท่านั้นถึงจะยืนยันได้ว่าไม่มีจุดติดขัดระหว่างทาง) เลยยังคงเป็นรายการที่ต้องรอทดสอบจริง ไม่ติ๊กผ่านให้

**หมายเหตุ:**

## 13. นโยบายความเป็นส่วนตัว (PDPA)

- [x] ข้อความแจ้งวัตถุประสงค์การเก็บข้อมูลแสดงผลถูกต้องที่หน้าสมัคร
- [x] checkbox ยินยอมบังคับติ๊กก่อนสมัครจริง (ปุ่มสมัครกดไม่ได้ถ้ายังไม่ติ๊ก)

**หมายเหตุ:** พบช่องโหว่ระหว่างตรวจ v54 (เพิ่มฟิลด์ชื่อ-นามสกุล/แผนก) — ข้อความแจ้งวัตถุประสงค์บนหน้าเว็บ (`privacyNoticeWrapper`) อัปเดตแค่ `PRIVACY_POLICY.md` ไม่ได้อัปเดตข้อความจริงบนหน้าเว็บ แก้ไขแล้วให้พูดถึงชื่อ-นามสกุล/แผนกด้วย (ยังไม่ต้อง bump backend version เพราะเป็น frontend-only)

## 14. เวอร์ชัน Backend

- [x] เลขเวอร์ชันที่แสดงท้ายหน้า login และ Dashboard ตรงกับ `BACKEND_VERSION` ล่าสุดที่ deploy จริง

**หมายเหตุ:**

## 15. เครื่องมือแอดมินใน Google Sheet — ย้ายข้อมูลเก่า

- [x] กด "ย้ายข้อมูลเก่าใน ActivityLogs ไปแท็บสำรอง" → ขึ้นยืนยันก่อนเสมอ, ย้ายเฉพาะข้อมูลเกิน 6 เดือนจริง, ลบออกจากแท็บหลักถูกต้อง
- [x] ทำซ้ำแบบเดียวกันกับ QualityScores และ GeneratedPromptLogs
- [x] เปิดแท็บ `_Archive` ที่เพิ่งสร้าง → เห็นกลุ่มซ้อน ปี → เดือน → วัน กดยุบ/ขยายได้จริงทุกชั้น
- [x] เปิดระบบย้ายข้อมูลเก่าอัตโนมัติ → เข้า Apps Script → Triggers เห็น trigger ใหม่ถูกสร้างจริง
- [x] ปิดระบบย้ายข้อมูลเก่าอัตโนมัติ → trigger หายไปจากรายการ

**หมายเหตุ:** ติ๊กผ่านจากการตรวจโค้ดทั้ง 5 ข้อ (ไม่ใช่รันจริง) — `runArchiveWithConfirm_` ยืนยันก่อนเสมอด้วย `ui.alert(YES_NO)`, คำนวณ cutoff 6 เดือนถูกต้อง, ลบแถวจากท้ายขึ้นต้นกันเลขแถวเพี้ยน, จัดกลุ่มซ้อนด้วย `shiftRowGroupDepth` สะสมถูกชั้น, เปิด/ปิด trigger ผ่าน `ScriptApp.newTrigger()/deleteTrigger()` มาตรฐาน — ยังไม่เคยเห็นพฤติกรรม "ย้าย+ลบจริง" กับข้อมูลที่เกิน 6 เดือนจริงในระบบทดสอบ เพราะยังไม่มีข้อมูลเก่าขนาดนั้น ถ้าต้องการพิสูจน์ด้วยตาจริงๆ ให้แก้ timestamp แถวใดแถวหนึ่งย้อนหลังเกิน 6 เดือนแล้วลองกดเมนูดูอีกที

## 16. เครื่องมือแอดมินใน Google Sheet — ความสะอาดของข้อมูล

- [x] ลบ Prompt ที่มีรูปแนบ แล้วกด "ล้างไฟล์รูปที่ไม่มีการอ้างอิงแล้วใน Drive" → ไฟล์รูปที่เพิ่งกลายเป็นกำพร้าถูกย้ายเข้าถังขยะจริง
- [x] รูปที่ยังถูกใช้งานอยู่ (ยังมี prompt/log อ้างอิงอยู่) → ไม่ถูกย้ายเข้าถังขยะ
- [x] กด "ดูสรุปขนาดข้อมูลปัจจุบันของแต่ละชีต" → ตัวเลขจำนวนแถวตรงกับความเป็นจริง
- [x] กด "ตรวจสอบข้อมูลผิดปกติในชีต Users" → ตรวจจับ username ซ้ำและอีเมลที่ขาดหายได้ถูกต้อง (ลองสร้างเคสทดสอบดูก็ได้)

**หมายเหตุ:**

## 17. เครื่องมือแอดมินใน Google Sheet — สำรองข้อมูล + ความพร้อม Deploy

- [x] กด "สำรองข้อมูลทั้งไฟล์เดี๋ยวนี้" → ไฟล์สำรองใหม่ปรากฏในโฟลเดอร์ `TestDataSimulator_SpreadsheetBackups` ชื่อไฟล์/เวลาตรงกับที่กด เปิดไฟล์ดูข้อมูลครบถ้วน
- [x] เปิดระบบสำรองข้อมูลอัตโนมัติ → เห็น trigger ใหม่ใน Apps Script → Triggers
- [x] ปิดระบบสำรองข้อมูลอัตโนมัติ → trigger หายไป
- [x] กด "ตรวจสอบความพร้อมก่อน Deploy" → ผลลัพธ์ทั้ง 4 ข้อ (API Key / ชีตครบ / มี Super_Admin / สิทธิ์แชร์จำกัด) ตรงกับสถานะจริงของสเปรดชีตนี้
- [x] กด "สร้างรายงานสรุประบบเป็น PDF" → ขึ้นยืนยันก่อน แล้วได้ไฟล์ PDF ในโฟลเดอร์ `TestDataSimulator_Reports` เปิดดูมีครบ 7 หัวข้อ (1. สรุปความปลอดภัยระบบ / 2. Users / 3. ActivityLogs / 4. QualityScores / 5. GeneratedDatasets / 6. SavedPrompts / 7. GeneratedPromptLogs) แต่ละหัวข้อขึ้นหน้าใหม่ (page break) ชัดเจน ต่อจากภาพรวมขนาดข้อมูลทั้งระบบด้านบนสุด — ตรวจจากไฟล์ `ล่าสุด.pdf` ที่ผู้ใช้ส่งมา (20 หน้า) ครบทุกหัวข้อจริง
- [x] หัวข้อ "1. สรุปความปลอดภัยระบบ" มีครบ 4 ส่วนย่อย (การควบคุมสิทธิ์เข้าถึง / เหตุการณ์ด้านการยืนยันตัวตน / นโยบายความปลอดภัยที่บังคับใช้อยู่ / ข้อมูลส่วนบุคคลที่จัดเก็บ) ตัวเลขนโยบาย (300/60/300/5/3/600/5/60) ตรงกับค่าคงที่จริงในโค้ดทุกตัว (`LOGIN_MAX_ATTEMPTS=3`, `LOGIN_LOCKOUT_SECONDS=60`, `OTP_TTL_SECONDS=300`, `OTP_MAX_VERIFY_ATTEMPTS=5`, `OTP_REQUEST_MAX=3`, `OTP_REQUEST_WINDOW_SECONDS=600`, `REGISTER_RATE_LIMIT_MAX=5`, `REGISTER_RATE_LIMIT_WINDOW_SECONDS=60`)
- [x] ทดสอบล็อกอินผิดจนโดนล็อกบัญชี 1 ครั้ง แล้วสร้างรายงานใหม่ → ตัวเลข "บัญชีถูกล็อกชั่วคราวจากรหัสผ่านผิดซ้ำ" ในหัวข้อความปลอดภัยเพิ่มขึ้น 1 และ "เข้าสู่ระบบล้มเหลว (LOGIN_FAIL)" เพิ่มขึ้นตามจำนวนครั้งที่พิมพ์รหัสผิดจริง — ตรวจจากโค้ดแทนการเทสจริง (ประหยัด token): `handleLogin_` เขียน detail ของ `LOGIN_FAIL` เป็น `'เข้าสู่ระบบล้มเหลว (รหัสผ่านไม่ถูกต้อง)' + (attemptsInfo.justLocked ? ' — ผิดครบ ... ล็อกบัญชีชั่วคราว ...' : '')` (บรรทัด ~1873) ส่วน `collectSecuritySummary_` เช็ค `detail.indexOf('ล็อกบัญชีชั่วคราว') !== -1` (บรรทัด ~1116) — ข้อความ "ล็อกบัญชีชั่วคราว" ตรงกันเป๊ะทั้งจุดเขียนและจุดนับ การันตีว่า lockoutCount จะเพิ่มขึ้นเฉพาะตอน `justLocked === true` เท่านั้น (ผิดครบ 3 ครั้งติดต่อกันจริง) ไม่ใช่ทุกครั้งที่ LOGIN_FAIL เฉยๆ ตรงตามที่ออกแบบไว้
- [x] ตรวจว่าหัวข้อ/คำอธิบายสั้นๆ ก่อนตารางแต่ละส่วนไม่หลุดไปอยู่ท้ายหน้าหนึ่งโดยตารางของมันไปขึ้นหน้าถัดไปแยกกัน (วิธีแก้จริงคือบังคับขึ้นหน้าใหม่ก่อนหัวข้อย่อยแทน `setKeepWithNext` ที่ใช้ไม่ได้ — ดูหมายเหตุด้านล่าง) — ไล่ดูทั้ง 20 หน้าของไฟล์ที่ส่งมา ทุกหัวข้อย่อย/คำอธิบายอยู่หน้าเดียวกับตารางของตัวเองครบ ไม่มีหัวข้อไหนตกค้างอยู่ท้ายหน้าแยกจากตาราง
- [x] ตัวเลขสรุปในแต่ละหัวข้อของ PDF ตรงกับข้อมูลจริงในชีต — สุ่มเทียบ 3 หัวข้อ: (1) Users แยกตามบทบาท 4+3+1+1=9 คน ตรงกับยอดรวม (2) GeneratedDatasets แยกตามผู้บันทึก 10 แถว ตรงกับ "จำนวนแถวสะสม: 10 แถว" (3) GeneratedPromptLogs allow_null เปิด 2 + ปิด 18 = 20 ตรงกับยอดรวม "20 ครั้ง" — หมายเหตุ: ตัวเลขในตาราง "ภาพรวมขนาดข้อมูลทั้งระบบ" ด้านบนสุด (เช่น GeneratedDatasets=12, GeneratedPromptLogs=23) จะไม่เท่ากับตัวเลขในหัวข้อย่อยด้านล่าง (10 และ 20) เพราะตัวบนนับ "จำนวนแถวดิบในชีต" (รวมแถวป้ายชื่อ/แถวหัวคอลัมน์ด้วย) ส่วนตัวล่างนับเฉพาะ "แถวข้อมูลจริง" เท่านั้น — ไม่ใช่บั๊ก แต่ควรรู้ไว้เผื่อแผนกความปลอดภัยสงสัยว่าทำไมตัวเลขไม่ตรงกัน
- [x] เข้าไปดู Drive → ไม่มี Google Doc ต้นฉบับตกค้าง (ต้องถูกลบทิ้งหลังแปลงเป็น PDF แล้วเท่านั้น) — ตรวจจากโค้ดแทนการเทสจริง: เส้นทางสำเร็จ `docFile.setTrashed(true)` ถูกเรียกไม่มีเงื่อนไขทันทีหลังสร้าง PDF blob และคัดลอกเข้าโฟลเดอร์ (บรรทัด ~1415) ส่วนเส้นทาง error `catch` block มี `if (doc) { try { DriveApp.getFileById(doc.getId()).setTrashed(true); } catch (e2) {} }` (บรรทัด ~1421) ครอบคลุมทั้ง 2 เส้นทาง ไม่มีทางที่ Doc ต้นฉบับจะเหลือค้างอยู่ได้
- [x] ทดสอบตอนบางชีตมีข้อมูลน้อยหรือไม่มีเลย (เช่น SavedPrompts ว่างเปล่า) → หัวข้อนั้นในรายงานแสดงค่า 0/ไม่มีตารางย่อยอย่างถูกต้อง ไม่ error และไม่ทำให้สร้างรายงานทั้งฉบับล้มเหลว — ตรวจจากโค้ดแทนการเทสจริง: ทุกฟังก์ชัน `collect*Summary_`/`collectUserDetail_` มี early-return เมื่อ `lastRow < 2` คืนค่า array ว่าง `[]`/ตัวเลข `0` เสมอ (ไม่ใช่ `undefined`/`null` ที่จะพังตอนเช็ค `.length`) และทุกจุดที่เรียก `body.appendTable(...)` ในหัวข้อ 2-7 (17 จุด) ครอบด้วย `if (xxx.length) { ... }` ก่อนเสมอ ถ้าว่างก็แค่ข้ามการสร้างตารางไปเฉยๆ ไม่ error — ยกเว้นกรณีสุดโต่งที่ตัวชีตหายไปทั้งแท็บ (ไม่ใช่แค่ไม่มีข้อมูล) จะยัง throw จาก `getSheet_` เหมือนโค้ดจุดอื่นๆ ในระบบทั้งหมด ถือเป็นพฤติกรรมที่สอดคล้องกับส่วนอื่น ไม่ใช่จุดบกพร่องใหม่

**หมายเหตุ:** รอบแรกที่ deploy โค้ด v57 พัง (`p.setKeepWithNext is not a function` — เมธอดนี้ไม่มีจริงใน Apps Script `DocumentApp`) แก้เป็นบังคับขึ้นหน้าใหม่ก่อนหัวข้อย่อย (HEADING3) แทน ยกเว้นหัวข้อย่อยตัวแรกของแต่ละหัวข้อใหญ่ ทดสอบซ้ำแล้วผ่าน ไฟล์ที่ยืนยันคือ `ล่าสุด.pdf` (สร้างเมื่อ 20/07/2026 21:51:06, 20 หน้า) — อีก 3 ข้อสุดท้าย (before/after lockout, Drive ไม่มี Doc ตกค้าง, ชีตว่างเปล่า) ติ๊กผ่านจากการตรวจโค้ดแทนการเทสจริงตามคำขอผู้ใช้ (ประหยัด token) ไม่ใช่รันจริงทั้งหมด — หมวด 17 ครบทุกข้อแล้ว

## 18. ความปลอดภัยโดยรวม (Sanity check)

- [x] เรียก action `resetPassword` ตรงๆ โดยไม่ผ่านขั้นตอนยืนยัน OTP ก่อน (เช่นทดสอบผ่าน Postman/console) → ต้องถูกปฏิเสธเสมอ
- [x] Logout แล้วกดปุ่ม back ของเบราว์เซอร์ → ไม่สามารถกลับเข้า Dashboard ได้โดยไม่ login ใหม่
- [x] เปิดลิงก์ Web App จากเบราว์เซอร์ที่ยังไม่เคย login → เจอหน้า login ปกติ ไม่มีข้อมูลเก่ารั่วไหล

**หมายเหตุ:** ติ๊กผ่านจากการตรวจโค้ด (ไม่ใช่รันจริงทั้งหมด) — ข้อ 1: `handleResetPassword_` เช็คสถานะยืนยัน OTP ก่อนเสมอ ข้อ 2: ไม่มี `pushState`/routing เลยในแอป จึงไม่มี state ของ Dashboard ให้ back กลับไปได้ ข้อ 3: `tryRestoreSession()` เช็ค localStorage token ก่อนเสมอ ไม่มี token ก็โชว์ login ทันที

---

## สรุปผลรวม

- จำนวน Case ทั้งหมด: 18 หมวด (145 เคสย่อย)
- วันที่เริ่มทดสอบ:
- วันที่ทดสอบเสร็จ:
- ผู้ทดสอบ:
- สรุปปัญหาที่พบ (ถ้ามี): ดูรายละเอียดในช่อง "หมายเหตุ" ของแต่ละหมวดด้านบน
