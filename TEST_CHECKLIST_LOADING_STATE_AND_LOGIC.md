# เช็คลิสต์ทดสอบ — Loading State (13 จุด) + Generation Logic Tracking

วันที่เตรียมไว้: 2026-09-09 | BACKEND_VERSION ที่เกี่ยวข้อง: v73-generation-logic-tracking-2026-09-09

> ก่อนทดสอบทุกข้อ: ต้อง Redeploy Web App ก่อน (Apps Script → Deploy → Manage deployments → New version → Deploy) ไม่ต้องกด Run ฟังก์ชันไหนเอง เช็คว่า deploy เวอร์ชันล่าสุดจริงผ่าน `?action=ping`

## ส่วนที่ 1: ทดสอบหน้ารอโหลด — กลุ่ม Toast "กำลังดำเนินการ" (กดยืนยันในกล่อง Confirm)

ทดสอบโดยกดปุ่ม → กดยืนยันในกล่อง Confirm → สังเกตว่าเห็นข้อความ "⏳ กำลังดำเนินการ..." ทันที (ไม่ใช่หน้าจอเงียบ) จนกว่าผลลัพธ์จริงจะกลับมา

- [x] 1. ออกจากระบบ (Sidebar → ออกจากระบบ) — ต้องเห็น "กำลังออกจากระบบ..." ก่อนสลับไปหน้า Login — **ผ่าน**
- [x] 2. ขอลบบัญชีตัวเอง — **ผ่าน** (ตัดสินใจแล้ว: ไม่ auto-logout หลังส่งคำขอ เพราะเป็นแค่คำขอรอ Super_Admin พิจารณา บัญชียังใช้งานได้ปกติ ไม่ใช่บั๊ก)
- [x] 3. อนุมัติคำขอสมัคร Super Admin (แผงจัดการผู้ใช้งาน) — **ผ่าน**
- [x] 4. ปฏิเสธคำขอสมัคร Super Admin — **ผ่าน**
- [x] 5. ยกเลิกคำขอลบบัญชี (Dismiss) — **ผ่าน**
- [x] 6. ลบผู้ใช้งาน (แผงจัดการผู้ใช้งาน) — **ผ่าน**
- [x] 7. ลบชุดข้อมูลที่เคย Commit (จากหน้า "ประวัติชุดข้อมูลที่เคย Commit") — **ผ่าน**
- [x] 8. ลบชุดข้อมูลที่เคย Commit (จากช่อง Reference Column Lock ตอนตั้งค่า Generate) — **ผ่าน**
- [x] 9. ลบ Prompt ที่บันทึกไว้ — **ผ่าน**

## ส่วนที่ 2: ทดสอบหน้ารอโหลด — กลุ่มปุ่ม Spinner + Disable (submit ในฟอร์ม/modal)

ทดสอบโดยกรอกฟอร์มแล้วกดปุ่ม submit → สังเกตว่าปุ่มถูก disable + ขึ้น spinner "กำลังบันทึก..." ทันที แล้วกลับมาเป็นปกติหลังได้ผลลัพธ์

- [x] 10. รีเซ็ตรหัสผ่านผู้ใช้งาน (admin) — **ผ่าน**
- [x] 11. ตั้ง/แก้ไขอีเมลผู้ใช้งาน (admin) — **ผ่าน**
- [x] 12. เปลี่ยนรหัสผ่านตัวเอง — **ผ่าน**
- [x] 13. บันทึก Prompt ปัจจุบัน (ปุ่ม "บันทึก" ข้างช่องตั้งชื่อ Prompt) — **ผ่าน**

## ส่วนที่ 3: หลอดเปอร์เซ็นต์ตอน Generate (ของเดิมที่แก้ไปก่อนหน้านี้ในรอบเดียวกัน)

- [x] กด "สร้างข้อมูลทดสอบ" แล้วสังเกตหลอดค่อยๆ ไต่ขึ้นช้าๆ (ไม่กระโดดไป 100% ทันทีตั้งแต่วินาทีแรก) และไปแตะ 100% ก็ต่อเมื่อผลลัพธ์จริงกลับมาจาก backend แล้วเท่านั้น — **ผ่าน**

**บั๊กที่เจอระหว่างทดสอบข้อนี้ (แก้แล้ว):** Rule-Based Mode เลือก generator ผิดสำหรับคอลัมน์ ID (ซ้ำกันข้ามแถว + ผูกกับชื่อคนละคน) และเลือกแม่แบบข้อความซ้ำกันบ่อยเกินไปตอนขอแถวน้อย — แก้ที่ `RuleCompilerPrompt.gs` (บังคับ ID ใช้ sequential + ปรับสูตรจำนวนแม่แบบขั้นต่ำ) และ `RuleDispatcher.gs` (เปลี่ยนวิธีเลือกแม่แบบเป็นสับไพ่+วนตามลำดับ) BACKEND_VERSION: v75-rule-compiler-template-shuffle-cycle-2026-09-10 — ทดสอบซ้ำแล้วผ่าน (customer_id 1-5 ไม่ซ้ำ, customer_name ทั้ง 5 แถวไม่ซ้ำ, นำเข้า Dashboard ได้)

## ส่วนที่ 4: ตรวจ Database (Google Sheet) — Generation Logic Tracking

- [x] 1. Generate ข้อมูล 1 รอบแบบ **ไม่ติ๊ก** Rule-Based Mode → เปิดชีต `QualityScores` และ `GeneratedPromptLogs` เช็คแถวล่าสุด คอลัมน์ `generation_logic` ต้องขึ้น **"Legacy (AI เต็มรูปแบบ)"** — **ผ่าน**
- [x] 2. Generate อีกรอบแบบ **ติ๊ก** Rule-Based Mode → คอลัมน์เดียวกันต้องขึ้น **"Rule-Based Mode"** — **ผ่าน**
- [x] 3. กด "บันทึก" Prompt ตอนติ๊ก Rule-Based Mode ไว้ → เปิดชีต `SavedPrompts` คอลัมน์ `use_rule_compiler` ต้องเป็น **TRUE** — **ผ่าน**
- [x] 4. โหลด Prompt จากข้อ 3 กลับมาใช้ (ดรอปดาวน์ "เลือก Prompt ที่จะโหลด") → toggle "Rule-Based Mode" บนฟอร์มต้องติ๊กอัตโนมัติตามที่เคยบันทึกไว้ — **ผ่าน**
- [x] 5. บันทึก Prompt อีกอันแบบ **ไม่ติ๊ก** แล้วโหลดกลับมา → toggle ต้อง **ไม่ติ๊ก** (กันเคสกลับด้าน/ค่าเพี้ยน) — **ผ่าน**

## สถานะ: ผ่านครบทุกข้อแล้ว ✅ (2026-09-10)

## หมายเหตุ

- ไฟล์ที่แก้ตลอดรอบนี้: `Code.gs`, `Final.html`, `index.html`, `RuleCompilerPrompt.gs`, `RuleDispatcher.gs` (HTML ทั้งสองไฟล์ซิงค์กันแล้ว ยืนยันด้วย diff)
- BACKEND_VERSION ล่าสุด: `v75-rule-compiler-template-shuffle-cycle-2026-09-10`
- ยังไม่ได้ push ขึ้น GitHub Pages — รอสั่งแยกต่างหาก
