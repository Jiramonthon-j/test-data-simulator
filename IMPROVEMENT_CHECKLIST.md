# เช็กลิสต์รายการที่เสนอเพิ่ม (Intelligent Test Data Simulator)

รายการที่เสนอไว้ตอนถามว่า "ควรเพิ่มอะไรอีกไหม" — ทำทีละข้อ อัปเดตสถานะในไฟล์นี้ทุกครั้งที่เพิ่มเสร็จ

- [x] **ส่งออกข้อมูลที่สร้างเป็น JSON** — เพิ่มปุ่ม "ดาวน์โหลด .json" ไว้ข้างปุ่ม "ดาวน์โหลด .sql" เดิมในหน้า Dashboard ใช้ข้อมูลชุดเดียวกับที่ export SQL (`pagesData.general.rows`) ตัดฟิลด์ภายใน `is_edited` ออกให้เหมือนกับ .sql/commit เดิม (ตอนแรกเคยเพิ่มปุ่ม .csv ไว้ด้วย แต่ถอดออกตามคำขอ เพราะข้อมูลซ้ำกับตารางที่เห็นบน Dashboard อยู่แล้ว)
- [x] **หน้าดูประวัติชุดข้อมูลที่เคย Commit** — เพิ่มปุ่ม "📦 ประวัติชุดข้อมูลที่เคย Commit" ข้างปุ่มรายงานคุณภาพเดิม เปิดหน้าต่างแบบ 3 ขั้นตอน (เลือกวันที่ → เลือกรอบ Commit ของวันนั้น → ดูตัวอย่างตาราง + ดาวน์โหลด .sql/.csv/.json ของรอบนั้น) จัดกลุ่มตามวันเหมือนรายงานคุณภาพข้อมูล หลังบ้านมี action ใหม่ 2 ตัว (`getGeneratedDatasetsList`, `getGeneratedDatasetBatch`) ที่แกะโครงสร้างชีต `GeneratedDatasets` แบบอ่านอย่างเดียว ไม่แตะ `handleCommit_`/โครงสร้างชีตเดิมเลย (v24-v25)
- [x] **แผงจัดการผู้ใช้สำหรับ Super_Admin** — เพิ่มปุ่ม "🛡️ จัดการผู้ใช้งาน" ที่หัวหน้า Dashboard (ซ่อนไว้ให้เห็นเฉพาะ role Super_Admin) เปิดแผงดูรายชื่อผู้ใช้ทั้งหมด พร้อมปุ่ม "รีเซ็ตรหัสผ่าน" (มีเงื่อนไขความปลอดภัยเหมือนหน้าสมัครเดิม) และ "ลบ" ต่อผู้ใช้ หลังบ้านเพิ่ม action ใหม่ 3 ตัว (`getAllUsers`, `deleteUser`, `adminResetPassword`) ที่เช็คสิทธิ์ Super_Admin จากข้อมูล role จริงในชีตเสมอ (ไม่เชื่อ role ที่หน้าเว็บส่งมา) กันลบตัวเองตอนล็อกอินอยู่ และกันลบ Super_Admin คนสุดท้าย ไม่แตะ `handleLogin_`/`handleRegister_`/`handleResetPassword_` เดิมเลย (v26)
- [x] **ตัวเช็ค/เตือนโควตา Gemini คร่าวๆ ก่อน generate** — เพิ่มแถบแสดง % การใช้งาน AI เหนือปุ่ม "สร้างข้อมูลทดสอบ" (เขียว/เหลือง/แดงตามสัดส่วน) หลังบ้านนับจำนวนครั้งที่เรียก Gemini จริงใน 60 วินาทีล่าสุดเทียบกับ limit ที่เคยเจอจริง (20 ครั้ง/นาที) เก็บผ่าน Script Properties — เป็นการประมาณการเอง ไม่ใช่ตัวเลขที่ดึงจาก Google โดยตรง (Google ไม่มี API เช็คโควตา free tier ให้) ไม่แตะ logic การ generate หรือ retry เดิมเลย แค่แอบนับจำนวนครั้งเพิ่มเข้าไปเฉยๆ (v27)
- [x] **แจ้งเตือนแบบ real-time ถ้าชื่อผู้ใช้งานซ้ำตอนสมัคร** — เดิม backend มี action `checkUserExists` อยู่แล้วแต่ไม่เคยถูกเรียกใช้จากหน้าเว็บเลย เพิ่มการเรียกใช้แบบ debounce 500ms ตอนพิมพ์ username ในหน้าลงทะเบียน (เช็คเฉพาะตอนผ่านเงื่อนไขรูปแบบเบื้องต้นแล้ว กันยิง backend ถี่เกินไป) ขึ้นข้อความเตือนเล็กๆ สีแดงใต้ช่องกรอกทันทีถ้าพบว่ามีชื่อนี้ในระบบแล้ว ไม่ได้แทนที่การตรวจสอบซ้ำฝั่ง backend ตอนกดยืนยันสมัครจริง (`handleRegister_` ยังปฏิเสธซ้ำอยู่เหมือนเดิม) เป็นแค่ชั้นเตือนล่วงหน้าให้ผู้ใช้เห็นก่อนกรอกข้อมูลอื่นจนครบ (frontend only ไม่ได้แก้ backend เลย)
- [x] **กันตั้งรหัสผ่านใหม่ซ้ำกับรหัสผ่านเดิม** — พบระหว่างเทส end-to-end ว่าเดิมระบบยอมให้ตั้งรหัสผ่านใหม่ซ้ำกับรหัสผ่านปัจจุบันได้ (ไม่มีการเทียบเลย) เพิ่มการเช็คใน `handleChangeOwnPassword_` (เปลี่ยนรหัสผ่านตัวเองขณะ login อยู่) และ `handleResetPassword_` (ตั้งรหัสผ่านใหม่ผ่าน OTP ลืมรหัสผ่าน) โดยคำนวณ hash ของรหัสใหม่ด้วย salt เดิมเทียบกับ hash เดิมที่เก็บไว้ ถ้าตรงกันปฏิเสธพร้อมข้อความ "รหัสผ่านใหม่ต้องไม่ซ้ำกับรหัสผ่านเดิม" ไม่ได้แก้ `handleAdminResetPassword_` (Super_Admin รีเซ็ตให้ผู้ใช้อื่น) เพราะเป็นคนละ use case (v51)
- [ ] **End-to-end checklist ทดสอบทุก flow หลักก่อนส่งงานจริง** — สร้างเช็กลิสต์ทดสอบละเอียดแยกไฟล์ไว้ที่ `E2E_TEST_CHECKLIST.md` แล้ว (18 หมวด ~95 เคสย่อย ครอบคลุมทั้งหน้าเว็บและเครื่องมือแอดมินใน Google Sheet) กำลังทดสอบอยู่ (หมวด 1-4 ผ่านครบ, หมวด 5 กำลังทำ)
- [x] **แก้บั๊ก: แนบรูป Schema/ER Diagram แล้ว Gemini ไม่ใช้รูปนั้นเลย** — พบระหว่างเทส end-to-end หมวด 5 (Generate): ทดสอบแนบรูป ER Diagram (ตาราง CUSTOMERS↔ORDERS ความสัมพันธ์ 1:N) ตอน generate ข้อมูล MongoDB แต่ผลลัพธ์ที่ได้กลับเป็นข้อมูล access_logs ที่ไม่เกี่ยวข้องกับรูปเลย ตรวจสอบพบว่าฝั่งหน้าเว็บส่งรูปไปให้ backend ถูกต้อง (`schemaImageBase64`) และ backend ก็แนบรูปนั้นเข้าไปในคำขอถึง Gemini จริง (เป็น `inline_data`) แต่ข้อความ prompt ที่ส่งไปพร้อมรูป (`buildSmartPrompt_`) ไม่เคยพูดถึงรูปที่แนบมาเลยสักคำ ทำให้ Gemini ได้รับรูปแต่ไม่รู้ว่าต้องใช้ทำอะไร จึงเมินรูปไป แก้โดยเพิ่มข้อความสั่งชัดเจนใน `buildSmartPrompt_` (เมื่อมี `p.schemaImageBase64` เท่านั้น) ให้ Gemini ใช้รูปเป็นข้อมูลอ้างอิงหลักของชื่อตาราง/คอลัมน์/ชนิดข้อมูล/PK-FK/ความสัมพันธ์ระหว่างตาราง โดยถ้ามี DDL Script ด้วยให้ยึด DDL เป็นหลักสำหรับชื่อคอลัมน์/ชนิดข้อมูล แต่ยังใช้รูปประกอบความเข้าใจความสัมพันธ์อยู่ดี (v52)
- [x] **บังคับเกณฑ์ขั้นต่ำของคะแนนคุณภาพก่อนนำเข้า Dashboard + ป้ายระดับข้อมูล** — เดิมหน้าจุดพัก (staging) เป็นแบบ "Human-in-the-Loop" ล้วนๆ คือปุ่ม "ยืนยันนำเข้าสู่ Dashboard" กดได้เสมอไม่ว่าผลตรวจสอบจะผ่านหรือไม่ก็ตาม ผู้ใช้แจ้งว่าต้องการให้บล็อกจริงถ้าคุณภาพต่ำเกินไป จึงเพิ่มเกณฑ์ขั้นต่ำ: ตรงตามเงื่อนไข (conditionMatchPercent) ต้อง ≥70% **และ** ความน่าเชื่อถือ (reliabilityPercent) ต้อง ≥90% ทั้งคู่ ถึงจะกดนำเข้าได้ ถ้าต่ำกว่านี้ปุ่มจะถูกบล็อกจริง (สีเทา กดไม่ได้) พร้อมข้อความอธิบายเหตุผล — กรณีตรวจสอบเชิงเนื้อหาล้มเหลวทางเทคนิค (ค่าเป็น N/A) ยังคงปล่อยผ่านแบบ advisory เหมือนเดิม เพราะประเมินเป็นตัวเลขไม่ได้ นอกจากนี้เพิ่มป้าย "ระดับข้อมูลที่สร้างได้" (พื้นฐาน/ปานกลาง/ขั้นสูง) คิดจากค่า**ต่ำสุด**ของสองคะแนน (ระวังไว้ก่อน ไม่ให้ตัวเลขตัวใดตัวหนึ่งสูงโดดมาบดบังอีกตัว) แบ่งช่วง: พื้นฐาน 70-84%, ปานกลาง 85-94%, ขั้นสูง 95-100% (เฉพาะตอนผ่านเกณฑ์ขั้นต่ำแล้วเท่านั้น) แสดงทั้งที่หน้าจุดพักและใต้ตาราง Dashboard พร้อมบันทึกคอลัมน์ `quality_level`/`pass_minimum_threshold` เพิ่มลงชีต `QualityScores` ด้วย (v53) — เพิ่มเติม: หน้าจุดพักเดิมแสดงแค่ ✅/❌ กับป้ายระดับ ไม่มีตัวเลข % ให้เห็นตรงๆ จึงเพิ่มแถวแสดง % ตรงตามเงื่อนไข และ % ความน่าเชื่อถือ (พร้อมเกณฑ์ขั้นต่ำกำกับ) ไว้บนหน้าจุดพักด้วยเลย ไม่ต้องรอไปดูใต้ Dashboard หลัง import แล้วเท่านั้น (frontend only ไม่กระทบ backend/v53)
- [x] **เพิ่มฟิลด์ชื่อ-นามสกุล และแผนก/ทีม ตอนสมัครสมาชิก** — เดิมสมัครสมาชิกเก็บแค่ username/email/password/role ทำให้ Super_Admin ดูรายชื่อผู้ใช้ในระบบแล้วไม่รู้ว่าเป็นใครจริงๆ หรือมาจากทีมไหน เพิ่ม 2 ฟิลด์บังคับกรอก: ชื่อ-นามสกุลจริง และแผนก/ทีมที่สังกัด เก็บลงคอลัมน์ใหม่ `full_name`/`department` ในชีต `Users` (เพิ่มแบบไม่ทำลายชีตเก่าด้วย `ensureColumnHeader_` เหมือนคอลัมน์ `email`) แสดงผลในแผงจัดการผู้ใช้งานของ Super_Admin ด้วย 2 คอลัมน์ใหม่ ส่วนการติดตามว่า user ทำอะไรบ้าง (ตามการเคลื่อนไหว) ใช้ชีต `ActivityLogs` ที่มีอยู่แล้วครอบคลุมอยู่แล้ว (บันทึกทุก action พร้อมเวลา) จึงไม่ได้เพิ่มกลไกติดตามใหม่ซ้ำซ้อน อัปเดต `PRIVACY_POLICY.md` ให้ระบุการเก็บข้อมูล 2 อย่างนี้ด้วย (v54)
- [x] **เพิ่มเครื่องมือแอดมิน "สร้างรายงานสรุประบบเป็น PDF"** — ก่อนเพิ่มได้ปรึกษากันแล้วว่า Google Sheets เองมี Pivot Table/Chart/Download ในตัวอยู่แล้วสำหรับรายงานทั่วไป แต่ยังไม่มีทางสรุปข้าม-หลายชีตเป็นรูปแบบพร้อมส่งให้คนที่ไม่ได้ใช้ Sheets (เช่นผู้บริหาร) ได้ในคลิกเดียว จึงเพิ่มเมนูใหม่นี้ที่รวบรวม (1) จำนวนแถวของแต่ละชีตหลัก (2) จำนวนผู้ใช้งานแยกตามแผนก/ทีม (ใช้ฟิลด์ `department` ที่เพิ่งเพิ่มไปใน v54 — ผู้ใช้เก่าก่อน v54 จะถูกจัดเป็น "ไม่ระบุ") (3) สรุปคุณภาพข้อมูลโดยรวมจาก `QualityScores` (จำนวนครั้งที่ generate ทั้งหมด, ค่าเฉลี่ย % ตรงตามเงื่อนไข/ความน่าเชื่อถือ เฉพาะแถวที่มีค่าเป็นตัวเลขจริง ไม่นับ N/A) สร้างเป็น Google Doc ก่อนแล้วแปลงเป็น PDF ด้วย `getAs(MimeType.PDF)` (Apps Script ไม่มี API สร้าง PDF ตรงๆ จากศูนย์) เก็บไว้ในโฟลเดอร์ Drive แยกต่างหาก `TestDataSimulator_Reports` แล้วลบ Google Doc ต้นฉบับทิ้งหลังแปลงเสร็จ กันขยะบวมใน Drive (v55)
- [x] **ขยายรายงาน PDF ให้ละเอียดที่สุดเท่าที่จะทำได้ แยกเป็นหัวข้อทีละชีต** — ผู้ใช้ทดสอบใช้งานจริงแล้วขอให้เพิ่มความละเอียด จากเดิมที่มีแค่ตารางจำนวนแถวรวม + สรุปแผนก + ค่าเฉลี่ยคุณภาพ 3 บรรทัด ปรับเป็นรายงาน 6 หัวข้อแยกทีละชีต (ขึ้นหน้าใหม่ทุกหัวข้อด้วย `appendPageBreak()`): **(1) Users** — แยกตามบทบาท, แยกตามแผนก/ทีม, จำนวนที่ยังไม่กรอกอีเมล/ชื่อ-นามสกุล, ตารางรายชื่อผู้ใช้ทั้งหมดพร้อมสถานะ **(2) ActivityLogs** — แยกตามประเภทกิจกรรม (action_type), ผู้ใช้ที่มีกิจกรรมมากสุด 10 อันดับ, ช่วงวันที่ที่มีบันทึก **(3) QualityScores** — เฉลี่ย/ต่ำสุด/สูงสุดของทั้ง % ตรงตามเงื่อนไขและ % ความน่าเชื่อถือ, จำนวนผ่าน/ไม่ผ่าน/N/A ตามเกณฑ์ขั้นต่ำ, แยกตาม dialect และประเภทข้อมูล, ผู้ใช้ที่สร้างข้อมูลมากสุด 10 อันดับ **(4) GeneratedDatasets** — แกะแถวป้ายชื่อ 📦 ของทุกรอบ Commit ด้วย `parseGeneratedDatasetBatches_()` ตัวเดียวกับที่หน้าเว็บใช้แสดงประวัติ (การันตีตัวเลขตรงกับที่ผู้ใช้เห็นบนเว็บเป๊ะๆ) สรุปจำนวนรอบ/แถวสะสม แยกตามผู้บันทึก/ประเภทข้อมูล/ชื่อตาราง (สูงสุด 10 อันดับ) **(5) SavedPrompts** — จำนวนที่บันทึกไว้, จำนวนที่มีรูปแนบ, แยกตาม dialect/ประเภทข้อมูล **(6) GeneratedPromptLogs** — จำนวน prompt ที่ส่งไปสร้างข้อมูลทั้งหมด, สัดส่วนเปิด/ปิด allow_null, แยกตาม dialect/ประเภทข้อมูล ทุกฟังก์ชันรวบรวมข้อมูลใหม่ (`collectUserDetail_`, `collectActivityLogSummary_`, `collectGeneratedDatasetsSummary_`, `collectSavedPromptsSummary_`, `collectGeneratedPromptLogsSummary_`) ข้ามแถวป้ายชื่อวันที่ 📅 อย่างถูกต้องเหมือนโค้ดเดิม ไม่แตะ logic การบันทึกข้อมูลของชีตใดๆ เลย เป็นแค่การอ่านสรุปเพิ่มเติม (v56)
- [x] **เพิ่มหัวข้อ "สรุปความปลอดภัยระบบ" ในรายงาน PDF เดิม + แก้ปัญหาหัวข้อ/ตารางแยกหน้ากัน** — ผู้ใช้ถามว่ารายงานที่มีอยู่ (v55/v56) ละเอียดพอจะส่งให้แผนกความปลอดภัยดูมั้ย คำตอบคือยังไม่พอ เพราะเนื้อหาเดิมเป็นสถิติการใช้งาน/คุณภาพข้อมูลล้วนๆ ไม่มีมิติความปลอดภัยเลย จึงเพิ่มหัวข้อใหม่ "1. สรุปความปลอดภัยระบบ" ไว้ในไฟล์ PDF เดียวกัน (ไม่แยกเป็นไฟล์ต่างหากตามที่ผู้ใช้ขอ) วางไว้ต่อจากภาพรวมขนาดข้อมูลทั้งระบบ ก่อนหัวข้อ Users (เลื่อนหมายเลขหัวข้อเดิม Users→ActivityLogs→...→GeneratedPromptLogs จาก 1-6 เป็น 2-7) เนื้อหาในหัวข้อใหม่ประกอบด้วย: (ก) การควบคุมสิทธิ์เข้าถึง — สถานะสิทธิ์แชร์สเปรดชีต, ตั้งค่า GEMINI_API_KEY แล้วหรือยัง, จำนวน Super_Admin, สถานะระบบย้ายข้อมูลเก่า/สำรองข้อมูลอัตโนมัติ (ใช้ตรรกะเดียวกับ `checkDeployReadiness()`) (ข) เหตุการณ์ด้านการยืนยันตัวตนจาก `ActivityLogs` — นับ LOGIN_SUCCESS/LOGIN_FAIL/จำนวนครั้งบัญชีถูกล็อกจริง (แยกจาก LOGIN_FAIL ทั่วไปด้วยการเช็คข้อความ "ล็อกบัญชีชั่วคราว" ใน detail)/OTP ที่ส่ง/RESET_PASSWORD/CHANGE_PASSWORD(_FAIL)/ADMIN_RESET_PASSWORD/ADMIN_SET_EMAIL/DELETE_USER/REGISTER (ค) นโยบายความปลอดภัยที่บังคับใช้จริง — ดึงค่าคงที่จริงจากโค้ดมาแสดง (`LOGIN_MAX_ATTEMPTS`, `LOGIN_LOCKOUT_SECONDS`, `OTP_TTL_SECONDS`, `OTP_MAX_VERIFY_ATTEMPTS`, `OTP_REQUEST_MAX`, `OTP_REQUEST_WINDOW_SECONDS`, `REGISTER_RATE_LIMIT_MAX`, `REGISTER_RATE_LIMIT_WINDOW_SECONDS`) แทนการพิมพ์ตัวเลขตายตัวแยกไว้ต่างหาก กันข้อมูลเพี้ยนถ้าค่าคงที่เปลี่ยนในอนาคต (ง) สรุปข้อมูลส่วนบุคคลที่จัดเก็บ (PDPA) อ้างอิงตาราง `PRIVACY_POLICY.md` — เพิ่มฟังก์ชันใหม่ `collectSecuritySummary_()` ให้ทำหน้าที่นี้โดยเฉพาะ นอกจากนี้ผู้ใช้ยังขอให้แก้ปัญหาหัวข้อ/คำอธิบายสั้นๆ ไปตกท้ายหน้าหนึ่งแล้วตารางที่ตามมาไปขึ้นหน้าถัดไปแยกกัน จึงเพิ่มตัวช่วย `reportHeading_()`/`reportParagraph_()` ที่ตั้งค่า `setKeepWithNext(true)` ให้ทุกย่อหน้า/หัวข้อในรายงาน (มีผลเฉพาะการแบ่งหน้าธรรมชาติ ไม่ยกเลิก `appendPageBreak()` ที่บังคับขึ้นหน้าใหม่ระหว่างหัวข้อใหญ่แต่ละหัวข้อ) ใช้แทน `body.appendParagraph(...)` ตรงๆ ทุกจุดในฟังก์ชัน `generateSystemReportPdf()` — **แก้เพิ่มหลัง deploy จริงแล้วเจอ error**: `Paragraph.setKeepWithNext` ไม่มีอยู่จริงใน Apps Script `DocumentApp` (มีเฉพาะใน Google Docs API ขั้นสูงที่ต้องเปิด Advanced Service เพิ่ม ไม่ใช่ built-in) จึงเปลี่ยนวิธีแก้ปัญหาหัวข้อย่อย/ตารางแยกหน้ากันเป็นบังคับขึ้นหน้าใหม่ก่อนหัวข้อย่อย (HEADING3) ทุกหัวข้อแทน ยกเว้นหัวข้อย่อยตัวแรกของแต่ละหัวข้อใหญ่ (กันหน้าเปล่าที่มีแค่หัวข้อใหญ่ลอยๆ อยู่คนเดียว) รับประกันว่าหัวข้อย่อยกับตารางของมันเริ่มต้นหน้าเดียวกันเสมอ แลกกับใช้จำนวนหน้าในรายงานมากขึ้น (v57)
- [x] **เพิ่มปุ่ม "ขอลบบัญชี" (self-service) + จำกัดจำนวนแถวต่อคำขอให้สมจริงขึ้น + เอกสารเพิ่มเติม (v58)** — หลังถามความเห็นเรื่อง "ระบบยังต้องเพิ่มอะไรอีกไหมถึงจะสมบูรณ์" ตกลงกันว่าจะทำ 4 ข้อจาก 6 ข้อที่เสนอไป (เก็บเรื่อง real-time anomaly alert กับโควตา Gemini จริงจาก GCP ไว้ก่อน): (1) **ปุ่ม "🗑️ ขอลบบัญชี"** ที่หัวหน้า Dashboard — ผู้ใช้ทั่วไปกดขอลบบัญชีตัวเองได้ แต่**ไม่ลบทันที** เป็นแค่คำขอ (action ใหม่ `requestDeleteAccount`) บันทึก log `REQUEST_DELETE_ACCOUNT` แล้วส่งอีเมลแจ้ง Super_Admin ทุกคนที่มีอีเมลผูกไว้ให้ไปกดลบเองผ่านแผงจัดการผู้ใช้งานเดิม (`handleDeleteUser_` ไม่ถูกแตะเลย) จำกัด 1 คำขอ/24 ชม./คน กันสแปม จงใจไม่ให้ลบทันทีเพื่อกันกดพลาด/กันคนร้ายลบบัญชีตัวเองปิดร่องรอย (2) **ลดเพดานจำนวนแถวต่อคำขอ generate จาก 1,000 เหลือ 300** (`MAX_ROWS_PER_GENERATE` ค่าใหม่) — พบว่า 1,000 สูงเกินกว่า `GEMINI_MAX_TOKENS` (16,000) จะรองรับได้จริงสำหรับตารางที่มีมากกว่า 2-3 คอลัมน์ คำขอแถวเยอะๆ จึงมักถูก AI ตัดคำตอบก่อนครบแล้ว fail อยู่ดี (โดนจับได้จาก `rows.length !== rowsRequested` เดิมอยู่แล้ว แต่เสีย execution time ไปฟรีๆ ก่อนรู้ผล) แก้ทั้งฝั่ง backend และ frontend (เพิ่ม hint text ใต้ช่องกรอกด้วย) (3) **เพิ่มหมายเหตุ ActivityLogs ไม่ tamper-proof** ทั้งใน `PRIVACY_POLICY.md` และในหัวข้อความปลอดภัยของรายงาน PDF — เป็นข้อจำกัดที่มากับสถาปัตยกรรม "ใช้ Sheet เป็น DB" ไม่ใช่บั๊ก แต่ควรแจ้งตรงๆ ว่าใครมีสิทธิ์แก้ไขสเปรดชีตโดยตรงแก้/ลบ log ได้โดยไม่ทิ้งร่องรอย (4) **เขียน Backup & Recovery Runbook** เพิ่มใน `DEPLOY_GUIDE.md` — ขั้นตอนกู้คืนข้อมูลทีละขั้นตอน (หาไฟล์สำรอง, ใครมีสิทธิ์สั่งกู้คืน, วิธีกู้คืนตามความรุนแรงของปัญหา 3 แบบ, ตรวจสอบหลังกู้คืน, แจ้งทีม) — ทั้ง (3) และ (4) เป็นเอกสารล้วนๆ ไม่กระทบโค้ด
- [x] **เพิ่มคิวคำขอลบบัญชีในแผงจัดการผู้ใช้งานของ Super_Admin (v59)** — หลัง deploy v58 จริง ผู้ใช้ (Super_Admin) ทดสอบแล้วสะท้อนว่าหน้า Dashboard ของ Super_Admin เองก็ยังเห็นแค่ปุ่ม "🗑️ ขอลบบัญชี" ทั่วไปเหมือนผู้ใช้ธรรมดา ไม่มีจุดไหนในระบบที่แสดงว่า "มี user ไหนส่งคำขอลบเข้ามาบ้าง" ให้กดจัดการได้ตรงๆ ต้องพึ่งอีเมลแจ้งเตือนอย่างเดียวซึ่งไม่พอสำหรับงานดูแลระบบจริง จึงเพิ่มคอลัมน์ใหม่ `delete_requested_at` ในชีต `Users` (เพิ่มแบบไม่ทำลายชีตเก่าด้วย `ensureColumnHeader_` เหมือนคอลัมน์อื่นๆ) — `handleRequestDeleteAccount_` แก้ให้จับ row index ของผู้ใช้ที่ส่งคำขอไว้ (เดิมอ่านแค่ข้อมูลแถวมาเทียบ ไม่เคยเก็บตำแหน่งแถวจริง) แล้วเขียนเวลาไว้ในคอลัมน์นี้ พร้อมห่อด้วย `withLock_` เพิ่มเพราะเริ่มมีการเขียนแถวจริงแล้ว (เดิมไม่มี lock เพราะแค่อ่าน+ส่งอีเมล) และกันส่งซ้ำถ้ามีคำขอค้างอยู่แล้วโดยไม่ต้องรอ cooldown 24 ชม.หมดก่อน `handleGetAllUsers_` ส่งฟิลด์ `deleteRequestedAt` กลับมาด้วย และเรียงผู้ใช้ที่มีคำขอค้างขึ้นบนสุดของรายการเสมอ เพิ่ม action ใหม่ `dismissDeleteAccountRequest` (Super_Admin เท่านั้น, ผ่าน `withLock_`) ให้ยกเลิกคำขอได้โดยไม่ลบบัญชี (ล้างคอลัมน์ + ล้าง cooldown cache ให้ผู้ใช้ส่งคำขอใหม่ได้ทันทีถ้าจำเป็น) บันทึก log ใหม่ `DISMISS_DELETE_ACCOUNT_REQUEST` ฝั่งหน้าเว็บ `renderAdminUserTable()` เพิ่มแบนเนอร์สรุปจำนวนคำขอค้าง, ไฮไลต์แถวสีแดงอ่อน + ป้าย "ขอลบบัญชีเมื่อ..." ให้ผู้ใช้ที่มีคำขอค้าง, ปุ่ม "ยกเลิกคำขอ" (เห็นเฉพาะแถวที่มีคำขอค้าง) ไม่แตะ `handleDeleteUser_`/ปุ่ม "ลบ" เดิมเลย ยังใช้ flow ลบจริงแบบเดิมทุกอย่าง (v59)
- [x] **ล็อกสิทธิ์ Super Admin หลัก + บังคับอนุมัติก่อนสมัคร Super_Admin ใหม่ (v60)** — ผู้ใช้ (Admin123) สะท้อน 2 จุด: (1) หน้า Dashboard ของ Admin123 เองก็ยังมีปุ่ม "🗑️ ขอลบบัญชี" ทั้งที่บัญชีนี้ต้องมีอยู่เสมอเพื่อดูแลระบบ (2) เดิม dropdown ตอนสมัครสมาชิกให้เลือก role "Super Admin" ได้อย่างอิสระ ไม่มีการตรวจสอบใดๆ เท่ากับใครก็สมัครเป็น Super_Admin ได้ทันทีโดยไม่มีใครอนุมัติ แก้ทั้ง 2 จุด: เพิ่มค่าคงที่ `PRIMARY_SUPER_ADMIN_USERNAME = 'Admin123'` (ทั้ง `Code.gs` และ `Final.html`) ไว้เป็นจุดเดียวที่ต้องแก้ถ้าจะเปลี่ยนตัว Super Admin หลักในอนาคต **(1) กันขอลบบัญชีตัวเอง** — `handleRequestDeleteAccount_` ปฏิเสธทันทีถ้า username ตรงกับ Super Admin หลัก ฝั่งหน้าเว็บซ่อนปุ่ม "🗑️ ขอลบบัญชี" ให้บัญชีนี้ไปเลย (สองชั้นเหมือนฟีเจอร์อื่นๆ ในระบบ) **(2) คำขอสมัคร Super_Admin ต้องรออนุมัติ** — เพิ่มคอลัมน์ใหม่ `approval_status` ในชีต `Users` (`''` = อนุมัติแล้ว/ไม่ต้องอนุมัติ ค่าเริ่มต้นของ role อื่นและบัญชีเก่า, `'pending'` = รออนุมัติ) `handleRegister_` ปรับให้สร้างแถวโดยอ้างอิงตำแหน่งคอลัมน์จาก header จริงเสมอแทน fixed array (กันคอลัมน์เพี้ยนถ้ามีคอลัมน์อื่นถูกแทรกมาก่อนจากฟีเจอร์อื่น) ถ้าเลือก role Super_Admin จะตั้ง `approval_status='pending'` แล้วส่งอีเมลแจ้งเฉพาะ Super Admin หลัก (ไม่ใช่ Super_Admin ทุกคน ผ่าน `getPrimarySuperAdminEmail_()`) แทนการสร้างบัญชีที่ใช้งานได้ทันที ผู้สมัครเห็นข้อความ "⏳ รอการอนุมัติจากระบบ" ทันทีหลังกดสมัคร (ไม่ใช่ข้อความสำเร็จปกติ) `handleLogin_` เช็ค `approval_status==='pending'` ก่อนสร้าง session เสมอ ถ้าติดสถานะนี้จะบล็อกพร้อมข้อความอธิบายชัดเจน (ไม่นับเป็นความผิดพลาดของ login lockout) เพิ่ม action ใหม่ 2 ตัว `approveSuperAdminRequest`/`rejectSuperAdminRequest` (ผ่าน `withLock_`) จำกัดสิทธิ์ให้เฉพาะ Super Admin หลักเท่านั้นด้วยฟังก์ชันใหม่ `isPrimarySuperAdmin_()` (แคบกว่า `isSuperAdmin_()` ทั่วไป เจตนาให้มีคนเดียวตัดสินใจ) อนุมัติ = ล้าง `approval_status` เป็นค่าว่าง + ส่งอีเมลแจ้งผู้สมัครว่า login ได้แล้ว ปฏิเสธ = ลบบัญชีที่ยังไม่เคยได้รับอนุมัติออกจากระบบไปเลย + ส่งอีเมลแจ้งผลเช่นกัน (กันบัญชีค้างเข้าระบบไม่ได้ตลอดไป) ฝั่งหน้าเว็บ `renderAdminUserTable()` เพิ่มแบนเนอร์/ไฮไลต์แถวสีม่วง + ปุ่ม "✅ อนุมัติ"/"❌ ปฏิเสธ" ให้เฉพาะตอน Super Admin หลัก login อยู่เท่านั้น (Super_Admin คนอื่นเห็นแค่ป้ายสถานะ กดปุ่มอนุมัติ/ปฏิเสธไม่ได้ ทั้งฝั่งหน้าเว็บซ่อนปุ่มและฝั่ง backend ปฏิเสธคำขอถ้าไม่ใช่ตัวจริง) เพิ่ม log กิจกรรมใหม่ `REGISTER_PENDING_APPROVAL`/`APPROVE_SUPER_ADMIN_REQUEST`/`REJECT_SUPER_ADMIN_REQUEST` (v60)
- [x] **ปิดช่องโหว่: Super_Admin คนอื่นยังลบบัญชี Admin123 ได้อยู่ (v61)** — ผู้ใช้ถามหลัง v60 ว่า "Super Admin ที่ไม่ใช่หลักสามารถลบได้ใช่มั้ย" ตรวจโค้ดแล้วพบว่า v60 บล็อกแค่ "Admin123 ขอลบบัญชีตัวเอง" (`handleRequestDeleteAccount_`) แต่ไม่เคยแตะ `handleDeleteUser_` (ปุ่ม "ลบ" ที่ Super_Admin คนอื่นใช้ลบผู้ใช้คนอื่นในแผงจัดการผู้ใช้งาน) ซึ่งเช็คแค่ "ห้ามลบตัวเองขณะ login อยู่" กับ "ห้ามลบ Super_Admin คนสุดท้าย" เท่านั้น ทำให้ถ้าระบบมี Super_Admin ≥2 คน ใครก็กดลบ Admin123 ทิ้งได้จริง ซึ่งขัดกับเจตนาที่ต้องการให้ Admin123 เป็น Super Admin หลักถาวร (เพราะสิทธิ์อนุมัติ Super_Admin ใหม่ผูกกับ username นี้ตรงๆ ถ้าถูกลบจะไม่มีใครอนุมัติได้อีกเลย) แก้โดยเพิ่มเช็ค `isPrimarySuperAdmin_(p.targetUsername)` ใน `handleDeleteUser_` ปฏิเสธทันทีไม่ว่าใครเป็นคนกด ฝั่งหน้าเว็บ `renderAdminUserTable()` ปิดปุ่ม "ลบ" (disabled + tooltip อธิบาย) ให้แถวของ Admin123 เสมอ ไม่ว่าใครเป็นคน login อยู่ (สองชั้นเหมือนฟีเจอร์อื่นในระบบ) (v61)
- [x] **แถวคำขอสมัคร Super_Admin ที่รออนุมัติ ไม่มีปุ่ม "ลบ" ธรรมดาอีกต่อไป ต้องผ่านปุ่ม "ปฏิเสธ" เท่านั้น (v62)** — ผู้ใช้ (Admin123) เสนอว่าที่แถวคำขอสมัคร Super_Admin ไม่ควรมีปุ่ม "ลบ" ซ้อนกับปุ่ม "✅ อนุมัติ"/"❌ ปฏิเสธ" อยู่แล้ว เพราะถ้ากดปุ่ม "ลบ" ธรรมดาแทนปุ่ม "ปฏิเสธ" จะลบบัญชีไปเฉยๆ โดยไม่ส่งอีเมลแจ้งผู้สมัครและไม่บันทึก log ที่ถูกต้อง แก้โดย **(1)** ฝั่งหน้าเว็บ `renderAdminUserTable()` ไม่แสดงปุ่ม "ลบ" เลยสำหรับแถวที่ `approvalStatus === 'pending'` (ทั้งฝั่ง Admin123 และ Super_Admin คนอื่น) เหลือแค่ปุ่ม "✅ อนุมัติ"/"❌ ปฏิเสธ" (เห็นเฉพาะ Admin123) หรือแค่ป้ายสถานะเฉยๆ (Super_Admin คนอื่น) **(2)** `handleDeleteUser_` เพิ่มเช็ค `approval_status==='pending'` ปฏิเสธคำขอลบผ่าน action `deleteUser` โดยตรง แนะนำให้ใช้ปุ่ม "ปฏิเสธ" แทน (กันข้ามปุ่มที่ถูกซ่อนไว้ผ่าน DevTools) **(3)** ปรับข้อความอีเมลปฏิเสธใน `handleRejectSuperAdminRequest_` ให้ชัดเจนขึ้นว่าลบทั้ง username/password ที่สมัครไว้แล้ว และเชิญให้สมัครสมาชิกใหม่ได้ทันที (เดิมบอกให้สมัคร role อื่นเท่านั้น ปรับเป็นสมัครใหม่ได้ทั่วไปเพราะ username เดิมใช้ซ้ำได้ทันทีหลังถูกลบ) (v62)
- [x] **แก้บั๊ก: สมัคร username ซ้ำหลังบัญชีเก่าถูกลบ แต่โดน cache เก่าเล่นงาน (v63)** — ผู้ใช้เจอว่ากด "ขอลบบัญชี" ของ user ที่เพิ่งสมัครใหม่ (ทุก role ไม่ใช่แค่ Super_Admin) แล้วขึ้น error "คุณเพิ่งส่งคำขอลบบัญชีไปแล้วเมื่อไม่นานนี้" ทั้งที่บัญชีนี้ไม่เคยส่งคำขอมาก่อนเลย ตรวจโค้ดพบสาเหตุ: cache กันสแปมคำขอลบบัญชี (`DELETE_ACCOUNT_REQUEST_CACHE_PREFIX_`) รวมถึง cache อื่นๆ ที่ผูกกับ username (login lockout, OTP) ถูก key ด้วย **username เฉยๆ ไม่เกี่ยวกับว่าบัญชีนั้นถูกลบไปแล้วหรือยัง** เดิม `handleDeleteUser_` (ปุ่ม "ลบ") และ `handleRejectSuperAdminRequest_` (ปุ่ม "ปฏิเสธ") ลบแถวในชีตทิ้งแต่ไม่เคยล้าง cache เหล่านี้เลย มีแค่ `handleDismissDeleteAccountRequest_` (ปุ่ม "ยกเลิกคำขอ") ที่ล้าง cache คำขอลบบัญชีอย่างเดียว ทำให้ถ้าใครเคยกด "ขอลบบัญชี" ด้วย username หนึ่ง แล้วบัญชีนั้นถูกลบไป (ไม่ว่าจะผ่านปุ่ม "ลบ" หรือ "ปฏิเสธ") แล้วมีคนสมัคร username เดิมซ้ำภายใน 24 ชม. บัญชีใหม่ (คนละบัญชีจริงๆ) จะโดน cache ของบัญชีเก่าที่ถูกลบไปแล้วบล็อกไปด้วย แก้โดยเพิ่มฟังก์ชันกลาง `clearUserScopedCaches_(username)` ล้าง cache ที่ผูกกับ username ทั้งหมด 9 ตัว (login lockout 2 ตัว, lockout notify, OTP 5 ตัว, คำขอลบบัญชี) เรียกใช้ทั้งใน `handleDeleteUser_` และ `handleRejectSuperAdminRequest_` ทันทีหลัง `sh.deleteRow(...)` สำเร็จทุกครั้ง (v63)
- [x] **ส่งอีเมลแจ้งผลคำขอ "ขอลบบัญชี" ทั้ง 2 กรณี ไม่ใช่แค่ตอนอนุมัติสมัคร Super_Admin (v64)** — ผู้ใช้ขอให้เพิ่ม: ไม่ว่า Super_Admin จะ "ทำตามคำขอ" (กดปุ่ม "ลบ" ลบบัญชีจริงตามคำขอ) หรือ "ปฏิเสธคำขอ" (กดปุ่ม "ยกเลิกคำขอ" ไม่ลบบัญชี) ต้องส่งอีเมลแจ้งเจ้าของบัญชีที่ส่งคำขอมาทั้งสองกรณี ถามผู้ใช้ก่อนว่าจะจำกัดสิทธิ์นี้ให้เฉพาะ Super Admin หลักเหมือน flow อนุมัติ Super_Admin หรือไม่ ผู้ใช้ตอบว่าเปิดให้ Super_Admin ทุกคนทำได้เหมือนเดิม (ไม่เปลี่ยนสิทธิ์) แก้โดย **(1)** `handleDeleteUser_` อ่านคอลัมน์ `delete_requested_at`/`email` เพิ่ม เพื่อเช็คว่าการลบครั้งนี้เป็นการทำตามคำขอ "ขอลบบัญชี" ของเจ้าของบัญชีเองหรือไม่ (ไม่ใช่การลบบัญชีด้วยเหตุผลอื่นของ Super_Admin) ถ้าใช่และมีอีเมลผูกไว้ จะส่งอีเมลแจ้งว่าคำขอได้รับอนุมัติแล้วและบัญชี/username/password ถูกลบไปแล้ว **(2)** `handleDismissDeleteAccountRequest_` อ่านคอลัมน์ `email` เพิ่ม ส่งอีเมลแจ้งว่าคำขอถูกยกเลิก บัญชียังอยู่และใช้งานได้ตามปกติ ทั้งสองจุดส่งอีเมลแบบ non-blocking (ส่งไม่สำเร็จก็ไม่ทำให้การลบ/ยกเลิกคำขอล้มเหลว แค่บันทึก log `_EMAIL_FAIL` เพิ่มไว้) ไม่กระทบการลบบัญชีทั่วไปที่ไม่ได้มาจากคำขอ "ขอลบบัญชี" (จะไม่มีอีเมลส่งในกรณีนั้น เพราะไม่มี `delete_requested_at` ให้เช็ค) (v64)
- [x] **Code review recheck ทั้งระบบตามคำขอผู้ใช้ ("Recheck ทั้งหมดของระบบ...ถ้าตรวจจากโค้ด") — พบและแก้บั๊กจริง 3 จุด (v65)** — ไล่อ่านโค้ดของฟีเจอร์ v59-v64 ทั้งหมดเทียบกับ `E2E_TEST_CHECKLIST.md` เจอ 3 ปัญหาที่ไม่มีใครรายงานมา แก้ครบทั้ง 3: **(1) อีเมลมี markdown หลงเหลือ** — `handleDismissDeleteAccountRequest_` (v64) ใช้ `**...**` ในเนื้อหาอีเมล ทั้งที่ `MailApp.sendEmail` ทั้งระบบเป็น plain text ล้วน (ไม่มี `htmlBody` ที่ไหนเลย) จะขึ้นเป็นดอกจันสองอันโชว์ตรงๆ ให้ผู้รับเห็น ไม่ได้ตัวหนาจริง — ลบ markdown ออก **(2) เอาคำขอสมัคร Super_Admin ที่ยังรออนุมัติไปนับเป็น Super_Admin ตัวจริง** — `getSuperAdminEmails_()` (แจ้งเตือนคำขอ "ขอลบบัญชี"), ตัวนับ `superAdminCount` ใน `handleDeleteUser_` (กันลบ Super_Admin คนสุดท้าย) และ `collectSecuritySummary_` (ตัวเลขในรายงาน PDF) ทั้ง 3 จุดกรองแค่ `role === 'Super_Admin'` โดยไม่เช็ค `approval_status` เลย ทำให้ผู้ที่สมัคร Super_Admin ไว้แต่ยังไม่ได้รับอนุมัติ (login เข้าไม่ได้ด้วยซ้ำ) ถูกนับเป็น Super_Admin ที่ทำงานได้จริง — ผลคือได้รับอีเมลแจ้งคำขอลบบัญชีของคนอื่นทั้งที่ยังไม่มีสิทธิ์อะไรเลย และตัวเลขในรายงาน/การป้องกันลบคนสุดท้ายคลาดเคลื่อน — เพิ่มเงื่อนไข `approval_status !== 'pending'` ในทั้ง 3 จุด **(3) ช่องโหว่ด้านสิทธิ์ที่ร้ายแรงกว่า: `isSuperAdmin_()` และ `checkDeployReadiness()` เจอปัญหาเดียวกัน** — `isSuperAdmin_()` เป็นจุดเช็คสิทธิ์เดียวที่ gate ทั้ง 5 action สงวนสิทธิ์ (`getAllUsers`, `deleteUser`, `dismissDeleteAccountRequest`, `adminResetPassword`, `adminSetUserEmail`) เดิมเช็คแค่ `role === 'Super_Admin'` จากแถวของ `requestingUsername` โดยไม่เช็ค `approval_status` เลย เท่ากับผู้ที่สมัคร Super_Admin ไว้แต่ยังไม่ได้รับอนุมัติจาก Admin123 (login ปกติเข้าไม่ได้) **สามารถยิง action สงวนสิทธิ์เหล่านี้ตรงๆ ผ่าน `requestingUsername` ของตัวเองได้ทันที โดยไม่ต้องรอการอนุมัติเลย** (ดูรายชื่อผู้ใช้ทั้งหมด ลบบัญชีคนอื่น รีเซ็ตรหัสผ่านคนอื่น หรือแก้อีเมลคนอื่นได้) ซึ่งขัดกับเจตนาทั้งหมดของฟีเจอร์อนุมัติ Super_Admin ใน v60 — แก้โดยเพิ่มเงื่อนไข `approval_status !== 'pending'` ใน `isSuperAdmin_()` เอง (จุดเดียวปิดครบทั้ง 5 action) เช่นเดียวกัน `checkDeployReadiness()` (ข้อ "มีผู้ใช้ role Super_Admin อย่างน้อย 1 คนไหม") ก็นับคำขอที่รออนุมัติเป็น Super_Admin ที่ใช้งานได้เหมือนกัน แก้ให้ตรงกัน — ทั้ง 3 บั๊กพบจากการ code review เชิงรุก ไม่ใช่จากรายงานของผู้ใช้ ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ ดู `E2E_TEST_CHECKLIST.md` รายการ **(v65)** ท้ายหมวด 11 (v65)

## เช็กลิสต์ก่อน Deploy ใช้งานจริง (เรียงตามความสำคัญ)

รายการที่ประเมินไว้ตอนถามว่า "พร้อม deploy จริงหรือยัง" — เรียงจากสำคัญที่สุดไปน้อยที่สุด อัปเดตสถานะทุกครั้งที่ทำเสร็จเหมือนลิสต์ด้านบน

- [x] **1. ล็อกสิทธิ์แชร์ Google Sheet ให้แน่นหนา** — ตั้งค่า "การเข้าถึงทั่วไป" ของสเปรดชีตเป็น "จำกัด" (Restricted) แล้ว เหลือเฉพาะรายชื่อที่แชร์ไว้ตรงๆ เท่านั้นที่เปิดดูได้ทันที คนอื่นที่ไม่ได้อยู่ในรายชื่อจะกดได้แค่ "ขอสิทธิ์เข้าถึง" ต้องรอเจ้าของไฟล์อนุมัติก่อนถึงจะเห็นเนื้อหาในชีต — **หมายเหตุ: ตอน deploy จริงจะสร้างสเปรดชีตใหม่ผูกกับ Gmail/API Key ของบริษัท ต้องมาตั้งค่านี้ซ้ำอีกรอบบนสเปรดชีตใหม่นั้นด้วย ไม่ใช่ทำครั้งเดียวจบตลอดไป**
- [x] **2. Rate limit ให้ `requestPasswordResetOtp`** — จำกัดไม่เกิน 3 ครั้ง/10 นาทีต่อ username หนึ่งคน ถ้าเกินล็อกไม่ให้ขอต่ออีก 10 นาที (เช็คก่อนแม้ username จะไม่มีจริงในระบบก็ตาม กันใช้เป็นช่องทางเดา/ยิงรัวๆ) เก็บผ่าน CacheService หมดอายุเองอัตโนมัติเหมือน login lockout (v42)
- [x] **3. Rate limit ให้ `register`** — จำกัดรวมทั้งระบบไม่เกิน 5 ครั้ง/60 วินาที (ไม่จำกัดต่อ username เพราะทุกครั้งเป็นชื่อใหม่) กันสคริปต์ยิงสมัครบัญชีปลอมรัวๆ (v42)
- [ ] **4. รัน end-to-end checklist ที่ค้างไว้ (ข้อด้านบน)** — ต้องทำก่อนปล่อยใช้งานจริงเสมอ เพราะผ่านการแก้ไขหลายรอบมาก มีโอกาสมี regression หลุดที่ยังไม่เจอ (ดูเช็กลิสต์ละเอียดที่ `E2E_TEST_CHECKLIST.md`)
- [x] **5. แจ้งเตือนอีเมลเมื่อบัญชีโดน login lockout** — ส่งอีเมลแจ้งเจ้าของบัญชีทุกครั้งที่โดนล็อกจากผิดรหัสผ่านครบ 3 ครั้งติดต่อกัน (ใช้ MailApp เดิมจากฟีเจอร์ OTP) จำกัดไม่เกิน 1 ฉบับ/ชั่วโมงต่อ username กันไม่ให้กลายเป็นช่องทางยิงรัวๆ จนโควต้าอีเมลของทั้งระบบหมด (v43)
- [x] **6. นโยบายความเป็นส่วนตัว/แจ้งวัตถุประสงค์เก็บข้อมูล (PDPA)** — เพิ่มข้อความแจ้งวัตถุประสงค์ + checkbox ยินยอมแบบบังคับติ๊กก่อนลงทะเบียนที่หน้าสมัคร พร้อมสร้างเอกสาร `PRIVACY_POLICY.md` แยกต่างหากอธิบายรายละเอียดเต็ม (v43)
- [x] **7. ทดสอบ mobile responsiveness** — ตรวจโค้ดแล้วพบ 2 จุดที่ล้นจอบนมือถือจริง: (1) แถบปุ่มบนหัว Dashboard (Dark Mode/จัดการผู้ใช้งาน/เปลี่ยนรหัสผ่าน/ออกจากระบบ/รีเซ็ตข้อมูล) ไม่มี `flex-wrap` แก้โดยเพิ่ม `flex-wrap` ให้ (2) แถวกรอกรหัสผ่านยืนยัน Commit + ปุ่ม "บันทึกลงฐานข้อมูล" ความกว้างคงที่ (`w-44`) เสี่ยงล้นจอแคบ แก้เป็น `w-full sm:w-44`/`w-full sm:w-auto` พร้อม `flex-wrap` จุดอื่นๆ (viewport meta, breakpoint หลัก, ตาราง/Modal ใหญ่) ตรวจแล้วรองรับอยู่แล้ว
- [x] **8. ย้ายข้อมูลเก่าไปแท็บสำรอง (เดิมเรียก Archive) + LockService** — (1) เพิ่มเมนู "เครื่องมือแอดมิน" 3 รายการ (ย้ายข้อมูลเก่าใน ActivityLogs/QualityScores/GeneratedPromptLogs ไปแท็บสำรอง) ย้ายบล็อกข้อมูลที่เก่ากว่า 6 เดือน (`ARCHIVE_THRESHOLD_MONTHS`) ไปแท็บ `<ชื่อชีต>_Archive` ในไฟล์ Google Sheet เดียวกันนี้ แล้วลบออกจากแท็บหลัก (ข้อมูลไม่หาย แค่ย้ายไปคนละแท็บในไฟล์เดิม) กดเองจากเมนู หรือเปิดให้รันอัตโนมัติทุกเดือนก็ได้ (ดูข้อ 9) ไม่รวม GeneratedDatasets/SavedPrompts เพราะโครงสร้างต่างกัน (2) เพิ่ม `withLock_()` ครอบ action ที่แก้ไข/ลบแถวโดยอ้างอิง row index ในเว็บแอป (`register`, `deletePrompt`, `deleteGeneratedDatasetBatch`, `deleteUser`, `adminResetPassword`, `adminSetUserEmail`, `changeOwnPassword`, `resetPassword`) กันชนกันถ้ามีคำขอพร้อมกัน ส่วน action ที่เรียกบ่อย/ไม่เสี่ยง (login, generate, commit) ไม่ครอบล็อกเพื่อไม่ให้ช้าลงโดยไม่จำเป็น (v44)
- [x] **9. เครื่องมือแอดมินเพิ่มเติม — จัดการข้อมูลสะอาด/ค้นหาสะดวก/กันระบบช้า** — เพิ่มในเมนู "เครื่องมือแอดมิน" อีก 5 อย่าง: (1) เปิด/ปิดระบบย้ายข้อมูลเก่าไปแท็บสำรองอัตโนมัติทุกวันที่ 1 ของเดือน (time-driven trigger รันแทนการกดเอง) (2) ล้างไฟล์รูป Schema/ER Diagram ที่ไม่มีการอ้างอิงแล้วใน Drive ไปถังขยะ (แก้ปัญหาไฟล์กำพร้าค้างจากตอนลบ Prompt/ชุดข้อมูลที่ไม่ได้ลบไฟล์ Drive ตามไปด้วย) (3) ดูสรุปขนาดข้อมูล (จำนวนแถว) ของทุกชีตหลักพร้อมเตือนเมื่อเกิน 3,000 แถว/ชีต (4) ตรวจสอบ username ซ้ำและผู้ใช้ที่ยังไม่มีอีเมลในชีต Users (5) แนะนำใช้ฟีเจอร์ Ctrl+F และ Filter views ของ Google Sheets เอง (ไม่ต้องเขียนโค้ดเพิ่ม) สำหรับค้นหา/กรองข้อมูลในทุกแท็บ (v45) — **v46: ปรับคำในเมนู/ข้อความแจ้งเตือนทั้งหมดจาก "เก็บถาวร" เป็น "ย้ายข้อมูลเก่าไปแท็บสำรอง" ให้เข้าใจง่ายและชัดเจนขึ้นว่าข้อมูลย้ายไปอยู่คนละแท็บในไฟล์เดิม ไม่ได้ออกไปที่ไหนภายนอก** — **v47: แท็บสำรองจัดกลุ่มซ้อน 3 ชั้น ปี → เดือน → วัน แบบยุบ/ขยายได้ (ใช้ `shiftRowGroupDepth`) ให้ทุกเครื่องมือที่ย้ายข้อมูลเก่า (กดเองทั้ง 3 เมนู และระบบอัตโนมัติรายเดือน) เขียนลงแท็บสำรองด้วยโครงสร้างนี้เหมือนกัน — ข้อจำกัด: แต่ละรอบที่ย้ายจะต่อท้ายแท็บสำรองเสมอ ไม่ย้อนไปรวมกลุ่มปี/เดือนเดิมจากรอบก่อนหน้า ถ้าปีเดียวกันถูกย้ายคนละรอบอาจเห็นหัวข้อปีซ้ำสองกลุ่ม ข้อมูลยังถูกต้องครบถ้วน**
- [x] **10. สำรองข้อมูลทั้งไฟล์ + ตรวจสอบความพร้อมก่อน Deploy** — เพิ่มในเมนู "เครื่องมือแอดมิน" อีก 2 อย่าง: (1) สำรองข้อมูลทั้งไฟล์ (คัดลอกสเปรดชีตทั้งไฟล์ ต่างจากข้อ 8-9 ที่ย้ายแค่บางส่วนไปแท็บอื่นในไฟล์เดิม) ไปเก็บที่โฟลเดอร์ Drive แยกต่างหาก `TestDataSimulator_SpreadsheetBackups` กดเองได้ทันที หรือเปิดให้รันอัตโนมัติทุกวันจันทร์ก็ได้ เก็บไว้ล่าสุด 8 ชุดเสมอ เกินนี้ลบตัวเก่าสุดทิ้งอัตโนมัติกันโฟลเดอร์บวม (2) ตรวจสอบความพร้อมก่อน Deploy — เช็ครวดเดียวว่าตั้งค่า `GEMINI_API_KEY` แล้วหรือยัง, มีชีตหลักครบทุกแท็บไหม, มีผู้ใช้ role Super_Admin อย่างน้อย 1 คนไหม, สิทธิ์การแชร์สเปรดชีตถูกตั้งเป็น "จำกัด" แล้วหรือยัง (เช็คผ่าน Drive API อัตโนมัติ) — ใช้ได้ทั้งตอนนี้และตอนย้ายไปสเปรดชีตจริงตอน Deploy โดยไม่ต้องแก้โค้ดเพิ่ม (v48)
- [x] **11. `OTP_REQUEST_LOCK_SECONDS` กลับเป็น 600 แล้ว** — เคยลดเหลือ 60 วินาทีชั่วคราว (v49) เพื่อทดสอบ end-to-end สะดวกขึ้น ทดสอบเสร็จแล้วเปลี่ยนกลับเป็น 600 วินาที (10 นาที) ตามค่าออกแบบเดิมทันที (v50) ไม่ปล่อยค้างไว้
