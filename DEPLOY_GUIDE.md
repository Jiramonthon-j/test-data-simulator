# คู่มือ Deploy — Intelligent Test Data Simulator

ระบบนี้ประกอบด้วย 2 ส่วน: หน้าเว็บ (`Final.html`) ที่คุณเปิดใช้งาน และ backend (`Code.gs`) ที่รันอยู่บน Google Apps Script ผูกกับ Google Sheet ตัวเดียวกัน ทำหน้าที่เก็บ AI API key อย่างปลอดภัย เรียก Gemini API (Google AI Studio — มี Free Tier ไม่ต้องผูกบัตรเครดิต) ตรวจสอบข้อมูล และอ่าน/เขียน Google Sheet ให้ทั้งหมด

## ภาพรวมสถาปัตยกรรม

```
หน้าเว็บ (Final.html)  --fetch(POST)-->  Google Apps Script Web App (Code.gs)  --> Gemini API (ฟรี)
                                                    |
                                                    v
                                             Google Sheet (ฐานข้อมูล)
                        Users / ActivityLogs / GeneratedDatasets / SavedPrompts /
                     GeneratedPromptLogs / ColumnSchemaConfig / QualityScores
```

---

## 🚀 Deploy ขึ้นระบบจริงด้วยบัญชี Google กลาง + GitHub Pages (Production)

หมวดนี้เขียนขึ้นเฉพาะสำหรับตอนที่จะเปิดให้ **คนอื่นเข้าใช้งานจริง** (ไม่ใช่แค่ทดสอบส่วนตัว) โดยแยกทุกอย่างออกจากบัญชีที่ใช้พัฒนา/ทดสอบเดิมไปอยู่ที่ **บัญชี Google กลาง** บัญชีเดียว (ไม่ผูกกับอีเมลส่วนตัวของใครคนใดคนหนึ่ง) พร้อมโฮสต์หน้าเว็บผ่าน **GitHub Pages**

**สิ่งที่ต้องทำเอง (ต้องใช้บัญชีของคุณ/ทีม ไม่มีใครทำแทนได้ รวมถึงผมด้วย):**

1. **สร้างบัญชี Google กลาง** — ไปที่ [accounts.google.com/signup](https://accounts.google.com/signup) สร้าง Gmail ใหม่ (แนะนำตั้งชื่อที่สื่อถึงทีม/โปรเจกต์ เช่น `yourteam.testdatasim@gmail.com`) ขั้นตอนนี้ต้องยืนยันตัวตน (เบอร์โทร/OTP) ซึ่งเป็นข้อมูลส่วนตัวที่ผมเข้าไม่ถึงและไม่ควรทำแทนใครเลยแม้จะเทคนิคทำได้ก็ตาม — ให้คุณหรือคนในทีมสร้างเองแล้วแจ้งกลับมาว่าพร้อมแล้ว จากนั้นทำตาม "ขั้นตอนที่ 1-3" ด้านล่างนี้ **โดย login ด้วยบัญชีกลางนี้ตลอด** (ไม่ใช่บัญชีส่วนตัว) จะได้ไฟล์ Google Sheet, Apps Script, API Key, โฟลเดอร์ Drive สำรอง/รายงาน ทั้งหมดอยู่ใต้บัญชีกลางเดียวกันหมด ไม่กระจัดกระจาย
2. ทำตาม **ขั้นตอนที่ 1** (สร้าง Sheet + วางโค้ด `Code.gs` + รัน `setupSheet()`) — เพราะเป็นสเปรดชีตใหม่เอี่ยม ระบบจะสร้างผู้ใช้เริ่มต้นให้แค่คนเดียวคือ `Admin123` (Super Admin หลัก) โดยอัตโนมัติอยู่แล้ว **เท่ากับ "ล้างข้อมูลเก่าเหลือแค่ Super Admin หลัก" ที่ต้องการเสร็จในตัวทันที** ไม่ต้องลบอะไรเพิ่มเอง (ห้ามเอาสเปรดชีตที่ใช้ทดสอบอยู่ตอนนี้ไปใช้ต่อ เพราะมีข้อมูลทดสอบ/บัญชีทดสอบเก่าปนอยู่)
3. ทำตาม **ขั้นตอนที่ 2** (ขอ Gemini API Key ใหม่จาก [aistudio.google.com/apikey](https://aistudio.google.com/apikey) **ด้วยบัญชีกลาง**) แล้วนำไปตั้งใน Script Properties — คีย์นี้จะเป็นคนละคีย์กับที่ใช้ทดสอบอยู่ตอนนี้ (แยกโควต้ากันชัดเจน ไม่ปนกัน)
4. ทำตาม **ขั้นตอนที่ 3** (Deploy เป็น Web App) จะได้ **Web App URL ใหม่** (คนละอันกับที่ `Final.html` ตัวทดสอบใช้อยู่ตอนนี้) — คัดลอกเก็บไว้ใช้ในขั้นตอนถัดไป
5. เข้าเมนู **เครื่องมือแอดมิน > ตรวจสอบความพร้อมก่อน Deploy** ในสเปรดชีตใหม่ ให้ผ่านครบทั้ง 4 ข้อก่อน (API Key / ชีตครบ / มี Super_Admin / สิทธิ์แชร์จำกัด) แล้วตั้งสิทธิ์แชร์สเปรดชีตเป็น **"จำกัด" (Restricted)** ตามข้อ 1 ของเช็กลิสต์ก่อน deploy ใน `IMPROVEMENT_CHECKLIST.md`

**สิ่งที่เตรียมไว้ให้แล้ว (ทำในเซสชันนี้):**

- สร้างไฟล์ **`index.html`** ขึ้นมาใหม่ (สำเนาจาก `Final.html` ทุกอย่างเหมือนกันทุกฟีเจอร์ครบ v65) — ตั้ง `BACKEND_URL` เป็นค่า placeholder `"PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE"` ไว้ล่วงหน้า เพื่อให้ตรงกับชื่อไฟล์ที่ GitHub Pages ต้องการ (`index.html` ที่ root ของ repo) **`Final.html` เดิมไม่ได้ถูกแตะเลย ยังชี้ไปที่ backend ทดสอบตัวเดิมใช้งานได้ปกติ** แยกกันเด็ดขาดกับไฟล์ที่จะขึ้นจริง
- เริ่มต้น git repository ในโฟลเดอร์โปรเจกต์นี้แล้ว (`git init` + commit แรกของโค้ดชุด v65) พร้อมให้ push ขึ้น GitHub ได้ทันทีที่คุณมี repo ปลายทาง (ดูขั้นตอนถัดไป)

**ขั้นตอนสุดท้ายที่ต้องทำเอง — เชื่อม `index.html` เข้ากับ Web App ใหม่ แล้วขึ้น GitHub Pages:**

1. เปิด `index.html` แก้บรรทัด `const BACKEND_URL = "PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE";` ให้เป็น Web App URL จริงที่ได้จากข้อ 4 ด้านบน (ถ้าลืมแก้ หน้าเว็บจะขึ้นเตือนเองทันทีตอนเปิดใช้งานว่า "ยังไม่ได้ตั้งค่าการเชื่อมต่อ Backend" กันพลาดไว้แล้ว)
2. ไปที่ [github.com/new](https://github.com/new) สร้าง repository ใหม่ (public ถ้าจะใช้ GitHub Pages แบบฟรี) เช่นชื่อ `test-data-simulator`
3. เปิด Terminal ในเครื่องคุณ (หรือให้ผมสั่งจากที่นี่ก็ได้ถ้าคุณให้ URL ของ repo เปล่ามา) รันคำสั่งต่อไปนี้ในโฟลเดอร์โปรเจกต์:
   ```bash
   git remote add origin https://github.com/<your-username>/<repo-name>.git
   git branch -M main
   git push -u origin main
   ```
4. ใน repo บน GitHub ไปที่ **Settings > Pages** เลือก **Branch: main**, **Folder: / (root)** แล้วกด **Save**
5. รอ 1-2 นาที จะได้ URL หน้าเว็บจริงรูปแบบ `https://<your-username>.github.io/<repo-name>/` — นี่คือ URL ที่ส่งให้คนอื่นใช้งานได้เลย
6. ทดสอบเปิด URL นั้น → สมัครสมาชิกทดสอบ 1 บัญชี → login → generate ข้อมูลลองสัก 1 รอบ → เช็คว่าข้อมูลเข้าไปอยู่ใน Google Sheet ของบัญชีกลางจริง ก่อนประกาศให้คนอื่นใช้งาน

> หมายเหตุความปลอดภัย: `index.html`/`Final.html` เป็นไฟล์ frontend ล้วนๆ ไม่มี API key หรือรหัสผ่านฝังอยู่เลย (Gemini API key อยู่ใน Script Properties ฝั่ง backend เท่านั้น) ดังนั้นการเอาขึ้น GitHub public repo ปลอดภัย ไม่มีความลับรั่วไหล — สิ่งเดียวที่ควรระวังคือ `BACKEND_URL` เอง ถึงจะเห็นได้จากใครก็ตามที่เปิด source ของหน้าเว็บ แต่ backend ฝั่ง Apps Script ก็ตรวจสิทธิ์จริงจากข้อมูลในชีตเสมออยู่แล้ว (ไม่เชื่อ role ที่หน้าเว็บส่งมา) จึงไม่ใช่ช่องโหว่

---

## ขั้นตอนที่ 1 — สร้าง Google Sheet และวางโค้ด backend

1. ไปที่ [sheets.google.com](https://sheets.google.com) สร้างสเปรดชีตเปล่าใหม่ ตั้งชื่อ เช่น `Test Data Simulator DB`
2. เมนู **Extensions > Apps Script** จะเปิดหน้าต่าง editor ขึ้นมา
3. ลบโค้ดเริ่มต้นในไฟล์ `Code.gs` ออกทั้งหมด แล้วคัดลอกเนื้อหาทั้งหมดจากไฟล์ `Code.gs` ที่แนบมาวางแทน กด บันทึก (Ctrl+S)
4. ที่แถบด้านบนของ editor เลือกฟังก์ชัน `setupSheet` จาก dropdown แล้วกด **Run** (▶)
   - ครั้งแรกจะมีหน้าต่างขอสิทธิ์ (Authorization required) — เลือกบัญชี Google ของคุณ แล้วกด **Advanced > ไปที่ [ชื่อโปรเจกต์] (ไม่ปลอดภัย)** เพื่ออนุญาต (เป็นเรื่องปกติสำหรับสคริปต์ที่เขียนเอง ไม่ได้แปลว่าอันตราย)
     - ตั้งแต่เวอร์ชันที่รองรับการบันทึกรูป Schema/ER Diagram ลง Google Drive เป็นต้นไป หน้าต่างขอสิทธิ์นี้จะขอสิทธิ์เข้าถึง Google Drive เพิ่มด้วย (ใช้เก็บไฟล์รูปที่แนบมาตอน Generate/บันทึก Prompt) กด Allow ได้ตามปกติ
   - เมื่อรันสำเร็จจะมี popup แจ้งว่าตั้งค่าเรียบร้อย และจะเห็น 7 แท็บใหม่เกิดขึ้นในสเปรดชีต: `Users`, `ActivityLogs`, `GeneratedDatasets`, `SavedPrompts`, `GeneratedPromptLogs`, `ColumnSchemaConfig`, `QualityScores`
   - `SavedPrompts` เก็บเฉพาะ prompt ที่กดปุ่ม "บันทึก" ตั้งชื่อไว้ใช้ซ้ำเองเท่านั้น (ดึงกลับมาใช้บนหน้าเว็บได้) ส่วน `GeneratedPromptLogs` เก็บ prompt เต็มทุกครั้งที่กด "สร้างข้อมูลทดสอบ" โดยไม่ได้ตั้งใจบันทึกไว้ใช้ต่อ ไว้เป็นประวัติตรวจสอบย้อนหลังเท่านั้น
   - ทั้งสองแท็บนี้จะมีคอลัมน์ `image_file_id` / `image_url` เพิ่มมาด้วย — ถ้ามีการแนบรูป Schema/ER Diagram ตอน Generate หรือตอนบันทึก Prompt ระบบจะอัปโหลดไฟล์รูปไปเก็บไว้ที่โฟลเดอร์ Drive ชื่อ `TestDataSimulator_SchemaImages` (สร้างอัตโนมัติ) แล้วบันทึกลิงก์ไว้ในคอลัมน์นี้ — สาเหตุที่ไม่แปะรูปเป็น base64 ตรงๆ ลง cell เพราะ Google Sheets จำกัดความยาวต่อ cell ไว้ที่ 50,000 ตัวอักษร ซึ่งรูปภาพมักยาวเกินนั้น ฝั่งหน้าเว็บสามารถกด "โหลดมาใช้" จากรายการ Prompt ที่บันทึกไว้ เพื่อดึงรูปเดิมกลับมาใส่ในฟอร์มได้อัตโนมัติโดยไม่ต้องอัปโหลดซ้ำ
   - ระบบจะสร้างผู้ใช้เริ่มต้นให้อัตโนมัติ: **username: `Admin123`  password: `SecurePassword!1`** (role: Super_Admin) — ควรเปลี่ยนรหัสผ่านหลังใช้งานจริงครั้งแรก โดยล็อกอินแล้วกดปุ่ม **"🔑 เปลี่ยนรหัสผ่าน"** ที่หัวหน้า Dashboard ได้เลยโดยไม่ต้องแก้ที่สเปรดชีต
   - แท็บ `Users` มีคอลัมน์ `email` ด้วย แต่ผู้ใช้เริ่มต้น (`Admin123`) จะยังไม่มีอีเมลผูกไว้ — ควรล็อกอินแล้วเข้าแผงจัดการผู้ใช้งาน (ปุ่ม "🛡️ จัดการผู้ใช้งาน") กด "ตั้ง/แก้ไขอีเมล" ให้ตัวเองก่อน จะได้ใช้ฟีเจอร์ "ลืมรหัสผ่าน" แบบ OTP ทางอีเมลได้ในอนาคต (ดูรายละเอียดในหัวข้อ "ระบบยืนยันตัวตนและความปลอดภัย" ด้านล่าง)

## ขั้นตอนที่ 2 — ตั้งค่า Gemini API Key (ฟรี ไม่ต้องผูกบัตรเครดิต)

1. ไปที่ [aistudio.google.com/apikey](https://aistudio.google.com/apikey) ล็อกอินด้วยบัญชี Google กด **Create API key** จะได้คีย์ทันทีโดยไม่ต้องผูกบัตรเครดิตหรือกรอกข้อมูลการเงินใดๆ (มี Free Tier ให้ใช้งานได้เลย มีข้อจำกัดเรื่องจำนวนคำขอต่อนาที/ต่อวัน)
2. กลับมาที่ Apps Script editor คลิกไอคอนรูปเฟือง **Project Settings** ทางซ้าย
3. เลื่อนลงไปที่ **Script Properties** กด **Add script property**
4. Property = `GEMINI_API_KEY`, Value = API key ที่ได้จากขั้นตอนที่ 1 แล้วกด **Save**

ค่านี้จะไม่ถูกฝังในหน้าเว็บหรือโค้ดที่ใครมองเห็นได้ — ปลอดภัยเพราะรันอยู่ฝั่งเซิร์ฟเวอร์ของ Apps Script เท่านั้น

> หมายเหตุ: Free Tier ของ Gemini จำกัดจำนวนคำขอต่อนาที/ต่อวัน (ปรับตามรุ่นโมเดล) ถ้าใช้งานถี่มากอาจเจอ error แจ้งว่าเกินโควต้าได้ ลองใหม่อีกครั้งหลังรอสักครู่

## ขั้นตอนที่ 3 — Deploy เป็น Web App

1. มุมขวาบนของ editor กด **Deploy > New deployment**
2. คลิกไอคอนรูปเฟืองข้าง "Select type" เลือก **Web app**
3. ตั้งค่า:
   - **Execute as:** Me (บัญชีของคุณ)
   - **Who has access:** **Anyone** (สำคัญมาก — ถ้าเลือก "Anyone with Google account" หน้าเว็บจะเรียก API ไม่ได้เพราะติด login redirect)
4. กด **Deploy** จะได้ **Web app URL** หน้าตาประมาณ `https://script.google.com/macros/s/xxxxxxxx/exec` — คัดลอกเก็บไว้

> ทุกครั้งที่แก้โค้ดใน `Code.gs` ต้องกด **Deploy > Manage deployments > แก้ไข (ไอคอนดินสอ) > Version: New version > Deploy** ใหม่ ไม่งั้นการแก้ไขจะไม่มีผลกับ URL เดิม

## ขั้นตอนที่ 4 — เชื่อมหน้าเว็บเข้ากับ backend

เปิดไฟล์ `Final.html` ค้นหาบรรทัด:

```js
const BACKEND_URL = "PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE";
```

แก้เป็น URL ที่ได้จากขั้นตอนที่ 3 เช่น:

```js
const BACKEND_URL = "https://script.google.com/macros/s/xxxxxxxx/exec";
```

บันทึกไฟล์ แล้วลองเปิดทดสอบ (เปิดไฟล์ในเบราว์เซอร์ตรงๆ ก็ทดสอบ login/generate ได้แล้ว ก่อนค่อย deploy ขึ้นเว็บจริง)

## ขั้นตอนที่ 5 — Deploy หน้าเว็บขึ้น Hosting จริง

เลือกวิธีใดวิธีหนึ่ง (ฟรีทั้งคู่):

**Netlify (ง่ายที่สุด)**
1. ไปที่ [app.netlify.com/drop](https://app.netlify.com/drop)
2. ลาก `Final.html` (เปลี่ยนชื่อเป็น `index.html` ก่อน) ไปวางในหน้านั้น
3. ได้ URL ใช้งานจริงทันที

**GitHub Pages**
1. สร้าง repository ใหม่ อัปโหลด `Final.html` (ตั้งชื่อ `index.html`)
2. Settings > Pages > เลือก branch `main` แล้ว Save
3. รอ 1-2 นาที จะได้ URL รูปแบบ `https://<username>.github.io/<repo>/`

---

## การจัดการ "โครงสร้างคอลัมน์ที่หลังบ้านกำหนด" (ColumnSchemaConfig)

แท็บ `ColumnSchemaConfig` ในสเปรดชีตคือจุดควบคุมว่าแต่ละประเภทข้อมูล/ตารางอนุญาตให้มีคอลัมน์อะไรได้บ้าง โดยไม่ต้องแก้โค้ดเลย แก้ในสเปรดชีตได้โดยตรง:

| คอลัมน์ | ความหมาย |
|---|---|
| `data_type_key` | ข้อความที่จะจับคู่กับช่อง "ข้อมูลที่ต้องการสร้าง" หรือ "ชื่อตาราง" ที่ผู้ใช้พิมพ์ในหน้าเว็บ |
| `max_columns` | จำนวนคอลัมน์สูงสุด (ใช้เป็นข้อมูลอ้างอิง) |
| `allowed_columns_csv` | รายชื่อคอลัมน์ที่อนุญาต คั่นด้วยจุลภาค — ถ้า AI สร้างคอลัมน์อื่นนอกชุดนี้มา ระบบจะตัดทิ้งอัตโนมัติ |
| `required_columns_csv` | คอลัมน์ที่ต้องมีเสมอ ถ้าขาดจะแจ้งเตือนที่จุดพัก |
| `notes` | บันทึกช่วยจำ |

ถ้าไม่มีแถวที่ตรงกับ `data_type` ที่ผู้ใช้พิมพ์ ระบบจะไม่ตัดคอลัมน์ใดๆ (ปล่อยผ่านตามที่ AI สร้างมาทั้งหมด) — เพิ่มแถวใหม่ในแท็บนี้ได้เรื่อยๆ ตามประเภทข้อมูลที่จะใช้งานจริง

---

## คะแนนคุณภาพข้อมูล (QualityScores)

ทุกครั้งที่กด "สร้างข้อมูลทดสอบ" สำเร็จ ระบบจะให้ Gemini ประเมินและบันทึกคะแนน 2 ค่าลงแท็บ `QualityScores` โดยอัตโนมัติ (ไม่ต้องกด commit ก็บันทึกแล้ว) เพื่อเก็บเป็นสถิติไว้ทำรายงานย้อนหลัง:

| คอลัมน์ | ความหมาย |
|---|---|
| `timestamp` | เวลาที่ generate |
| `username`, `role` | ผู้ที่กด generate |
| `data_type`, `table_name`, `dialect` | เงื่อนไขที่ใช้ในรอบนั้น |
| `rows_requested`, `rows_actual` | จำนวนแถวที่ขอ vs ได้จริง |
| `condition_match_percent` | % ที่ข้อมูลตรงตามเงื่อนไขทั้งหมดที่ตั้งไว้ (DDL, ค่าที่อนุญาต, ค่าว่าง, ช่วงเวลา ฯลฯ) |
| `reliability_percent` | % ความน่าเชื่อถือของข้อมูล (ความสมจริง, ความสอดคล้องเชิงตรรกะ, ไม่ซ้ำซาก) |
| `summary` | สรุปผลสั้นๆ จาก AI |

ตัวเลข 2 ค่านี้จะแสดงเป็นแถบสีใต้ตาราง Dashboard บนหน้าเว็บด้วย (เขียว = 90% ขึ้นไป, เหลือง = 70-89%, แดง = ต่ำกว่า 70%) — เอาแท็บนี้ไปทำ Pivot Table หรือกราฟใน Google Sheets ต่อได้ทันทีเพราะเป็นตารางแบนราบ คอลัมน์คงที่ไม่เปลี่ยนตามชนิดข้อมูลที่สร้าง

กดปุ่ม **"📊 รายงานคุณภาพข้อมูลย้อนหลัง"** ใต้ตาราง Dashboard บนหน้าเว็บ จะเปิดหน้าต่างแสดงประวัติคะแนนทั้งหมดจาก `QualityScores` ให้ติ๊กเลือกเฉพาะรายการที่ต้องการ แล้วส่งออกเป็นไฟล์ `.csv` เพื่อนำไปทำสไลด์/รายงานนำเสนอได้ทันที

---

## Flow การทำงานเมื่อกดปุ่ม "สร้างข้อมูลทดสอบ"

1. หน้าเว็บประกอบเงื่อนไขทั้งหมด → ส่งไปที่ Apps Script (`action: generate`)
2. Apps Script สร้าง "smart prompt" จากเงื่อนไข + คำแนะนำคอลัมน์ที่อนุญาต แล้วเรียก Gemini API
3. Apps Script ตัด/ตรวจคอลัมน์ตาม `ColumnSchemaConfig`, ตรวจจำนวนแถว/ค่าว่าง, และเรียก Gemini API อีกครั้งเพื่อ "ตรวจทาน" ว่าเนื้อหาข้อมูลตรงเงื่อนไขที่ตั้งไว้จริงไหม
4. ผลลัพธ์ทั้งหมดถูกส่งกลับมา **"พัก" ที่หน้าจอ Modal จุดตรวจสอบ** บนเว็บ (ยังไม่เข้า Dashboard) พร้อมรายงานผล
5. ผู้ใช้ตรวจสอบรายงาน แล้วกด **"ยืนยันนำเข้าสู่ Dashboard"** เอง (Human-in-the-Loop) — ถึงจะเข้าตาราง preview จริง
6. เมื่อพอใจกับข้อมูล กดปุ่ม **"บันทึกลงฐานข้อมูล"** (ต้องกรอกรหัสผ่านซ้ำเพื่อ Commit) → ข้อมูลถูกเขียนลง `GeneratedDatasets` ใน Google Sheet จริง พร้อม log กิจกรรมทุกขั้นตอนใน `ActivityLogs`

---

## ระบบยืนยันตัวตนและความปลอดภัย (Session / Auto Logout / Login Lockout)

- **Session token + Auto Logout อัตโนมัติ**: ตอน login สำเร็จ backend จะออก session token (เก็บด้วย `CacheService`, อายุ 10 นาที) แล้วหน้าเว็บเก็บ token นี้ไว้ใน `localStorage` เพื่อกู้คืนสถานะ login ได้เองถ้าปิด/เปิดแท็บใหม่ภายในเวลาที่ยังไม่หมดอายุ ระบบนับเวลาไม่มีการใช้งาน (idle timeout) — ถ้าไม่มีการขยับเมาส์/พิมพ์/สกรอลล์/คลิกเลยเกิน **10 นาที** จะถูกออกจากระบบอัตโนมัติเพื่อความปลอดภัย แต่ถ้ามีการใช้งานต่อเนื่อง session จะถูกต่ออายุให้เรื่อยๆ ไม่ต้อง login ใหม่
- **จำกัดจำนวนครั้ง login ผิดพลาด (Login Lockout)**: ถ้ากรอกรหัสผ่านผิดติดต่อกัน **3 ครั้ง** สำหรับ username เดียวกัน ระบบจะล็อกไม่ให้ลอง login ซ้ำเป็นเวลา **60 วินาที** (นับจากครั้งที่ผิดครั้งล่าสุดที่ทำให้ครบ 3 ครั้ง) ปุ่ม "เข้าสู่ระบบ" จะเปลี่ยนเป็นสีเทาและกดไม่ได้ พร้อมนับถอยหลังบนตัวปุ่มจนครบเวลาแล้วกลับมากดได้เอง ตัวนับจะหมดอายุเองอัตโนมัติผ่าน `CacheService` ไม่ต้องเคลียร์ข้อมูลเอง
- **กู้คืนรหัสผ่านด้วย OTP ทางอีเมล ("ลืมรหัสผ่าน")**: เดิมฟีเจอร์นี้ยอมให้ตั้งรหัสผ่านใหม่ได้ทันทีแค่รู้ username ที่มีอยู่จริง ซึ่งเป็นช่องทางบายพาส login lockout ได้ง่ายๆ ตั้งแต่เวอร์ชันนี้เป็นต้นไปจึงเปลี่ยนเป็น 3 ขั้นตอน: (1) กรอก username → ระบบส่งรหัส OTP 6 หลักไปยังอีเมลที่ผูกไว้กับบัญชีนั้น (อายุ 5 นาที ใช้ได้ครั้งเดียว กรอกผิดเกิน 5 ครั้งต้องขอรหัสใหม่ — กดลิงก์ "ขอรหัสใหม่" ที่หน้ากรอก OTP ได้เลยไม่ต้องย้อนกลับไปเริ่มใหม่) (2) กรอกรหัส OTP ที่ได้รับมายืนยัน (3) ตั้งรหัสผ่านใหม่ได้จริงก็ต่อเมื่อผ่านขั้นตอนที่ 2 มาแล้วเท่านั้น — ตั้งรหัสผ่านสำเร็จจะปลดล็อก login lockout ที่อาจติดค้างอยู่ให้อัตโนมัติด้วย (เพราะยืนยันตัวตนผ่านอีเมลจริงแล้ว)
- **จำกัดอัตราการขอ OTP และการสมัครสมาชิก (Rate Limiting)**: ขอ OTP ได้ไม่เกิน **3 ครั้ง/10 นาที** ต่อ username หนึ่งคน (เกินแล้วล็อกไม่ให้ขอต่ออีก 10 นาที) กันไม่ให้ใครยิงขอ OTP รัวๆ จนโควต้าส่งอีเมลของทั้งระบบ (~100 ฉบับ/วัน) หมดเร็วเกินไป ซึ่งจะกระทบผู้ใช้ทุกคนพร้อมกัน ส่วนการสมัครสมาชิกใหม่ (`register`) จำกัดรวมทั้งระบบไม่เกิน **5 ครั้ง/60 วินาที** กันสคริปต์ยิงสมัครบัญชีปลอมรัวๆ จนชีต `Users` รก
- **สมัครสมาชิกต้องใช้ Gmail (gmail.com) เท่านั้น**: ระบบตรวจทั้งฝั่งหน้าเว็บและฝั่ง backend ว่าอีเมลต้องลงท้ายด้วย `@gmail.com` เท่านั้น กันพิมพ์โดเมนผิด (เช่น `gamil.com`) ที่ผ่านการตรวจรูปแบบอีเมลทั่วไปได้เพราะยังนับเป็นอีเมลที่ถูกต้องตาม RFC — ข้อจำกัดนี้ใช้เฉพาะตอนสมัครบัญชีใหม่เท่านั้น Super_Admin ยังตั้งอีเมลโดเมนอื่นให้ผู้ใช้ที่มีอยู่แล้วผ่านแผงจัดการผู้ใช้งานได้ตามปกติ
  - **อีเมลบังคับกรอกตอนสมัครบัญชีใหม่** ตั้งแต่เวอร์ชันนี้เป็นต้นไป (ใช้ MailApp ของ Apps Script ส่งอีเมล จาก mailbox ของบัญชี Google ที่ deploy Web App นี้ไว้ — มีโควต้าส่งอีเมลประมาณ 100 ฉบับ/วันสำหรับบัญชี Gmail ฟรีทั่วไป มากกว่านั้นถ้าเป็น Google Workspace)
  - **ผู้ใช้ที่สมัครไว้ก่อนเวอร์ชันนี้ (รวมถึง Admin123 ที่ระบบสร้างให้อัตโนมัติ) จะยังไม่มีอีเมลผูกในระบบ** และจะกู้คืนรหัสผ่านเองผ่าน OTP ไม่ได้จนกว่าจะมีอีเมล — ให้ Super_Admin เข้าแผงจัดการผู้ใช้งานแล้วกดปุ่ม **"ตั้ง/แก้ไขอีเมล"** เพิ่มให้ (ตั้งให้ผู้ใช้อื่น หรือตั้งให้ตัวเองก็ได้ ไม่มีข้อจำกัด)
- **เปลี่ยนรหัสผ่านตัวเองระหว่าง login อยู่**: กดปุ่ม **"🔑 เปลี่ยนรหัสผ่าน"** ที่หัวหน้า Dashboard ได้ทุกเมื่อ ต้องกรอกรหัสผ่านปัจจุบันให้ถูกต้องก่อน แล้วตั้งรหัสผ่านใหม่ตามเงื่อนไขความปลอดภัยเดียวกับตอนสมัคร (≥6 ตัวอักษร, มีตัวพิมพ์ใหญ่/เล็ก, มีอักขระพิเศษ)
- **แผงจัดการผู้ใช้สำหรับ Super_Admin**: ปุ่ม **"🛡️ จัดการผู้ใช้งาน"** (เห็นเฉพาะ role Super_Admin) ใช้ดูรายชื่อผู้ใช้ทั้งหมด ตั้ง/แก้ไขอีเมล รีเซ็ตรหัสผ่านผู้ใช้คนอื่น หรือลบผู้ใช้ได้ (ระบบกันไม่ให้ลบตัวเองตอน login อยู่ และกันไม่ให้ลบ Super_Admin คนสุดท้าย)
- **ขอให้ลบบัญชีตัวเอง (self-service, v58)**: ผู้ใช้ทั่วไปกดปุ่ม **"🗑️ ขอลบบัญชี"** ที่หัวหน้า Dashboard ได้ทุกเมื่อ — ระบบ**ไม่ลบบัญชีให้ทันที** แค่บันทึกคำขอลง `ActivityLogs` (action `REQUEST_DELETE_ACCOUNT`) แล้วส่งอีเมลแจ้ง Super_Admin ทุกคนที่มีอีเมลผูกไว้ให้เข้าไปพิจารณาลบเองผ่านแผงจัดการผู้ใช้งาน จงใจออกแบบเป็น "คำขอ" ไม่ใช่ "ลบทันที" เพื่อกันกดพลาดโดยไม่ตั้งใจและกันคนร้ายลบบัญชีตัวเองเพื่อปิดร่องรอยหลังทำอะไรผิดไว้ — จำกัดไม่เกิน 1 คำขอ/24 ชั่วโมงต่อคน กันสแปมคำขอ
- **คิวคำขอลบบัญชีในแผงจัดการผู้ใช้งาน (v59)**: Super_Admin ไม่ต้องพึ่งอีเมลแจ้งเตือนอย่างเดียวอีกต่อไป — เปิดแผง **"🛡️ จัดการผู้ใช้งาน"** แล้วจะเห็นแบนเนอร์สรุปจำนวนคำขอที่รอดำเนินการ พร้อมแถวผู้ใช้ที่ขอไว้ถูกไฮไลต์สีแดงอ่อนและเรียงขึ้นบนสุดเสมอ กด **"ลบ"** ตรงแถวนั้นได้ทันทีเหมือนแถวทั่วไป หรือกด **"ยกเลิกคำขอ"** (ปุ่มใหม่ เห็นเฉพาะแถวที่มีคำขอค้าง) เพื่อล้างคำขอโดยไม่ลบบัญชี ถ้ายกเลิกแล้วเจ้าของบัญชีส่งคำขอใหม่ได้ทันทีไม่ต้องรอครบ 24 ชม.
- **Super Admin หลักของระบบ + คำขอสมัคร Super_Admin ต้องรออนุมัติ (v60)**: บัญชี **`Admin123`** (ที่ระบบสร้างให้อัตโนมัติตอนติดตั้ง — ดู `PRIMARY_SUPER_ADMIN_USERNAME` ใน `Code.gs` ถ้าต้องเปลี่ยนตัวในอนาคต) เป็น Super Admin "หลัก" ของระบบ ไม่มีปุ่ม "🗑️ ขอลบบัญชี" ให้กดเลย (ทั้งซ่อนที่หน้าเว็บและปฏิเสธซ้ำที่ backend) — การสมัครสมาชิกใหม่ที่เลือก role **"Super Admin"** จะไม่ได้ใช้งานได้ทันทีเหมือน role อื่น ระบบจะสร้างบัญชีไว้แต่ตั้งสถานะ "รออนุมัติ" พร้อมส่งอีเมลแจ้ง Admin123 โดยเฉพาะ ผู้สมัครจะเห็นข้อความ "รอการอนุมัติจากระบบ" ทันทีและ login ไม่ได้จนกว่า Admin123 จะเข้าแผง **"🛡️ จัดการผู้ใช้งาน"** แล้วกด **"✅ อนุมัติ"** (ส่งอีเมลแจ้งผู้สมัครว่า login ได้แล้ว) หรือ **"❌ ปฏิเสธ"** (ลบบัญชีที่ยังไม่อนุมัติทิ้งไปเลย พร้อมอีเมลแจ้งผล) ปุ่มอนุมัติ/ปฏิเสธนี้เห็นเฉพาะตอน Admin123 login อยู่เท่านั้น Super_Admin คนอื่นเห็นแค่ป้ายสถานะ "⏳ รอการอนุมัติ" เฉยๆ
- **Admin123 ลบไม่ได้เด็ดขาด ไม่ว่าใครกด (v61)**: v60 บล็อกแค่ Admin123 ขอลบบัญชีตัวเองผ่านปุ่ม "ขอลบบัญชี" แต่ Super_Admin คนอื่นยังกดปุ่ม "ลบ" ในแผงจัดการผู้ใช้งานลบบัญชี Admin123 ทิ้งได้อยู่ (ถ้าระบบมี Super_Admin ≥2 คน) v61 ปิดช่องนี้เพิ่ม — ปุ่ม "ลบ" ที่แถวของ Admin123 เป็นสีเทากดไม่ได้เสมอไม่ว่าใคร login อยู่ และ `handleDeleteUser_` ปฏิเสธคำขอลบ Admin123 ซ้ำอีกชั้นที่ backend ด้วย
- **แถวคำขอสมัคร Super_Admin ไม่มีปุ่ม "ลบ" ธรรมดาอีกต่อไป (v62)**: แถวที่ยังรออนุมัติเหลือแค่ปุ่ม "✅ อนุมัติ"/"❌ ปฏิเสธ" (Admin123 เท่านั้น) หรือป้ายสถานะเฉยๆ (Super_Admin คนอื่น) กันการกดปุ่ม "ลบ" ธรรมดาแทนปุ่ม "ปฏิเสธ" โดยไม่ตั้งใจ ซึ่งจะลบบัญชีไปโดยไม่ส่งอีเมลแจ้งผู้สมัคร — กด **"❌ ปฏิเสธ"** จะลบ username/password ที่สมัครไว้ทิ้งทั้งหมดเสมอ พร้อมส่งอีเมลแจ้งผู้สมัครและเชิญให้สมัครสมาชิกใหม่ได้ทันที (username เดิมใช้ซ้ำได้เพราะถูกลบออกจากระบบแล้ว)
- **แก้บั๊ก username ซ้ำหลังบัญชีถูกลบแล้วโดน cache เก่าเล่นงาน (v63)**: cache ที่ผูกกับ username (login lockout, OTP, คำขอลบบัญชี) เดิมไม่เคยถูกล้างตอนลบบัญชีทั้งผ่านปุ่ม "ลบ" และปุ่ม "ปฏิเสธ" ทำให้ถ้าสมัคร username เดิมซ้ำภายใน 24 ชม. หลังบัญชีเก่าถูกลบ บัญชีใหม่ (คนละบัญชีจริง) จะโดน cache ของบัญชีเก่าบล็อก เช่น กด "ขอลบบัญชี" ครั้งแรกแล้วขึ้น error ว่า "เพิ่งส่งคำขอไปแล้ว" ทั้งที่ไม่เคยส่งมาก่อน — v63 แก้ให้ลบบัญชีทุกครั้ง (ทั้งปุ่ม "ลบ" และ "ปฏิเสธ") ล้าง cache ที่เกี่ยวข้องทั้งหมดของ username นั้นไปด้วยเสมอ
- **ส่งอีเมลแจ้งผลคำขอ "ขอลบบัญชี" ทั้ง 2 กรณี (v64)**: ไม่ว่า Super_Admin (คนไหนก็ได้ ไม่ได้จำกัดเฉพาะ Admin123) จะกดปุ่ม **"ลบ"** ทำตามคำขอ (บัญชีถูกลบจริง) หรือกดปุ่ม **"ยกเลิกคำขอ"** (ปฏิเสธ ไม่ลบ) เจ้าของบัญชีที่ส่งคำขอ "ขอลบบัญชี" มาจะได้รับอีเมลแจ้งผลเสมอทั้งสองกรณี (ถ้ามีอีเมลผูกไว้) — อีเมลนี้ส่งเฉพาะกรณีที่การลบเป็นการทำตามคำขอ "ขอลบบัญชี" ของเจ้าของบัญชีเองเท่านั้น การที่ Super_Admin ลบผู้ใช้ทั่วไปด้วยเหตุผลอื่น (ไม่มีคำขอค้างอยู่) จะไม่มีอีเมลนี้ส่งออกไป
- **เช็กเวอร์ชัน backend ที่ deploy อยู่จริง**: หน้าเว็บจะโชว์เลขเวอร์ชัน backend ปัจจุบันไว้ท้ายฟอร์ม login และท้ายหัวข้อ Dashboard โดยอัตโนมัติ (ดึงผ่าน action `ping`) มีประโยชน์ตอนเช็กว่า deploy เวอร์ชันล่าสุดสำเร็จหรือยัง ถ้าต้องการเช็กตรงๆ ก็เปิด `<BACKEND_URL>?action=ping` ในเบราว์เซอร์ได้เช่นกัน
- **แก้ช่องโหว่: คำขอสมัคร Super_Admin ที่ยังรออนุมัติใช้สิทธิ์ Super_Admin ได้ก่อนได้รับอนุมัติจริง (v65)**: พบระหว่างทำ code review recheck ทั้งระบบ (ไม่มีผู้ใช้รายงานเข้ามา) — `isSuperAdmin_()` ซึ่งเป็นจุดเช็คสิทธิ์เดียวที่ใช้ gate ทุก action สงวนสิทธิ์ในแผงจัดการผู้ใช้งาน (`getAllUsers`, `deleteUser`, `dismissDeleteAccountRequest`, `adminResetPassword`, `adminSetUserEmail`) เดิมเช็คแค่ `role === 'Super_Admin'` โดยไม่สนใจว่า `approval_status` เป็น `'pending'` อยู่หรือไม่ ทำให้ผู้ที่เพิ่งสมัคร role Super_Admin ไว้แต่ Admin123 ยังไม่ได้กด "✅ อนุมัติ" (login ผ่านหน้าเว็บปกติไม่ได้เลย) ยิง action เหล่านี้ตรงๆ ผ่าน `requestingUsername` ของตัวเองได้ทันที เท่ากับข้ามขั้นตอนอนุมัติทั้งหมดไปเลย แก้โดยเพิ่มเงื่อนไข `approval_status !== 'pending'` ใน `isSuperAdmin_()` — เช็คเดียวกันนี้ยังพบใน `getSuperAdminEmails_()` (ทำให้ผู้สมัครที่รออนุมัติได้รับอีเมลแจ้งคำขอลบบัญชีของคนอื่นทั้งที่ยังไม่มีสิทธิ์อะไร), ตัวนับ `superAdminCount` ใน `handleDeleteUser_`/`collectSecuritySummary_` (ตัวเลขป้องกันลบ Super_Admin คนสุดท้าย/รายงาน PDF คลาดเคลื่อน) และ `checkDeployReadiness()` (ข้อ "มี Super_Admin อย่างน้อย 1 คนไหม" อาจขึ้น ✅ ทั้งที่มีแค่คำขอรออนุมัติ) — แก้ครบทั้งหมดด้วยเงื่อนไขเดียวกัน นอกจากนี้ยังแก้อีเมลใน `handleDismissDeleteAccountRequest_` (v64) ที่มี markdown `**...**` หลงเหลืออยู่ทั้งที่อีเมลทั้งระบบเป็น plain text ล้วน ทุกจุดผ่านการตรวจ syntax (`node --check`) แล้ว แต่ยังไม่ได้ทดสอบจริงผ่านหน้าเว็บ

## แถบแสดงโควตาการเรียก AI (ใต้ปุ่ม "สร้างข้อมูลทดสอบ")

แถบนี้แสดง % การเรียก Gemini API จริงในช่วง 60 วินาทีล่าสุด เทียบกับลิมิต 20 ครั้ง/นาที (ค่าที่เจอจริงบน Free Tier) โดยอ้างอิงจากเวลาที่เรียก API จริงแต่ละครั้งที่ backend บันทึกไว้ (ไม่ใช่การกะประมาณ) และจะไล่ลด % เองแบบ real-time ทุกวินาทีตามเวลาจริงที่ผ่านไป — ถ้า backend เจอ error โควต้าเกิน (HTTP 429) จากฝั่ง Google จริงๆ แถบจะสลับไปโหมด "รอ Cooldown" พร้อมนับถอยหลังตามเวลาที่ Google แจ้งมาจริง (`retryDelay`) ไม่ใช่ค่าคงที่ที่เดาไว้ล่วงหน้า

หมายเหตุ: การกด "สร้างข้อมูลทดสอบ" 1 ครั้ง = เรียก Gemini จริง **2 ครั้ง** เสมอ (1. สร้างข้อมูล 2. ให้ AI ตรวจทานเนื้อหาอีกรอบก่อนส่งกลับมาที่จุดพัก) แถบโควตาจึงลดลง 2 ขีดทุกครั้งที่กดปุ่มนี้

## จัดการ Prompt ที่บันทึกไว้ / ประวัติชุดข้อมูลที่เคย Commit

- **Prompt ที่บันทึกไว้ (ใช้ซ้ำได้)**: นอกจากกด "โหลดมาใช้" แล้ว ยังกดปุ่มลบ (🗑️) ข้างๆ เพื่อลบ prompt ที่ไม่ใช้แล้วออกได้ ระบบจะลบทั้งแถวในแท็บ `SavedPrompts` ออกจาก Google Sheet ด้วย ไม่ใช่แค่ซ่อนที่หน้าเว็บ
- **ประวัติชุดข้อมูลที่เคย Commit**: ในหน้าต่างเลือกวันที่ → เลือกรอบ Commit จะมีปุ่มลบให้แต่ละรอบเช่นกัน กดแล้วจะลบข้อมูลทั้งบล็อกของรอบนั้น (ป้ายชื่อรอบ + หัวตาราง + ทุกแถวข้อมูล) ออกจากแท็บ `GeneratedDatasets` จริง ใช้ล้างข้อมูลทดสอบเก่าที่ไม่ใช้แล้วให้ Sheet ไม่รก

## ย้ายข้อมูลเก่าไปแท็บสำรอง (เดิมเรียก "Archive") + ป้องกันการชนกันของคำขอพร้อมกัน (LockService)

- **เมนู "เครื่องมือแอดมิน" ในหน้า Google Sheet**: เปิด Google Sheet ที่ผูกกับระบบนี้ จะเห็นเมนู "เครื่องมือแอดมิน" เพิ่มขึ้นมา 3 รายการใหม่: "ย้ายข้อมูลเก่าใน ActivityLogs / QualityScores / GeneratedPromptLogs ไปแท็บสำรอง" — กดแล้วระบบจะถามยืนยัน แล้วย้ายข้อมูลที่เก่ากว่า **6 เดือน** (ค่าคงที่ `ARCHIVE_THRESHOLD_MONTHS` ใน `Code.gs`) ไปเก็บไว้ที่แท็บสำรอง**ในไฟล์ Google Sheet เดียวกันนี้** ชื่อ `<ชื่อชีตเดิม>_Archive` (เช่น `ActivityLogs_Archive`) แล้วลบข้อมูลนั้นออกจากแท็บหลัก — **ข้อมูลไม่หายไปไหนและไม่ได้ออกไปที่ไหนภายนอก แค่ย้ายไปอยู่คนละแท็บในไฟล์เดิม** ยังเปิดดูย้อนหลังได้ตามปกติที่แท็บสำรองนั้น
- **ต้องรันเอง หรือเปิดให้รันอัตโนมัติก็ได้**: กดจากเมนูเป็นระยะๆ เอง (แนะนำทุก 3-6 เดือน หรือเมื่อเริ่มสังเกตว่าระบบช้าลง) หรือเปิดระบบอัตโนมัติทุกเดือนก็ได้ (ดูหัวข้อถัดไป) เหตุผลที่ยังไม่จำเป็นต้องรีบทำตอนนี้เพราะข้อมูลยังน้อย แต่ทุก action เกือบทั้งหมดอ่านข้อมูลทั้งชีตทุกครั้ง ยิ่งชีตมีแถวเยอะ ยิ่งอ่านช้าลงเรื่อยๆ การย้ายข้อมูลเก่าออกจึงช่วยให้ชีตหลักมีขนาดเล็ก อ่านเร็วอยู่เสมอ
- **ใช้ได้เฉพาะ 3 ชีตนี้**: `ActivityLogs`, `QualityScores`, `GeneratedPromptLogs` เพราะมีโครงสร้างการจัดกลุ่มแบบเดียวกัน (แถวป้ายชื่อวันที่ตามด้วยแถวข้อมูล) — ไม่รวม `GeneratedDatasets` (จัดกลุ่มแบบรอบ Commit คนละโครงสร้าง) และ `SavedPrompts` (ผู้ใช้ยังเรียกกลับมาใช้ซ้ำได้ ไม่ควรถูกย้ายออกอัตโนมัติ)
- **แท็บสำรองจัดกลุ่มซ้อน 3 ชั้น ปี → เดือน → วัน (v47)**: ข้อมูลในแท็บสำรองไม่ได้เรียงเป็นแถวยาวๆ เฉยๆ แต่จัดเป็นกลุ่มแบบยุบ/ขยายได้เหมือนแท็บหลัก — กดปุ่ม [－] ที่ขอบซ้ายเพื่อยุบทั้งปี ยุบทั้งเดือน หรือยุบทีละวันได้ ช่วยไล่ดูข้อมูลเก่าจำนวนมากได้ง่ายขึ้นโดยไม่ต้องเลื่อนดูทีละแถว **ข้อควรทราบ**: การย้ายข้อมูลแต่ละรอบจะเขียนต่อท้ายแท็บสำรองเสมอ ไม่ได้ย้อนไปรวมกลุ่มปี/เดือนที่เคยสร้างไว้จากรอบก่อนหน้า ถ้าข้อมูลปีเดียวกันถูกย้ายมาคนละรอบ (เช่น ย้ายเดือนมกราคมรอบหนึ่ง แล้วย้ายเดือนกรกฎาคมอีกรอบ) อาจเห็นหัวข้อปีนั้นซ้ำสองกลุ่มในแท็บสำรอง แต่ข้อมูลยังถูกต้องครบถ้วน แค่ไม่ได้รวมเป็นกลุ่มเดียวให้อัตโนมัติ
- **ป้องกันการชนกันของคำขอพร้อมกัน (LockService)**: เพิ่มการล็อกด้วย `LockService` ครอบ action ฝั่งเว็บแอปที่แก้ไข/ลบแถวโดยอ้างอิงตำแหน่งแถว (row index) ได้แก่ สมัครสมาชิก, ลบ Prompt, ลบชุดข้อมูลที่เคย Commit, ลบผู้ใช้, รีเซ็ตรหัสผ่านโดย Admin, ตั้ง/แก้ไขอีเมลโดย Admin, เปลี่ยนรหัสผ่านตัวเอง, และตั้งรหัสผ่านใหม่ (ลืมรหัสผ่าน) — ป้องกันกรณีมี 2 คำขอเข้ามาพร้อมกันแล้วอ้างอิงเลขแถวที่เลื่อนไปแล้วจากอีกคำขอหนึ่ง จนแก้/ลบผิดแถว ส่วน action ที่เรียกบ่อยมากและไม่เสี่ยงเรื่องนี้ (login, สร้างข้อมูลทดสอบ, commit) จงใจไม่ครอบล็อก เพื่อไม่ให้ผู้ใช้งานพร้อมกันหลายคนต้องรอคิวโดยไม่จำเป็น

## เครื่องมือแอดมินเพิ่มเติม — จัดการข้อมูลให้สะอาด/ค้นหาสะดวก/กันระบบช้า

เมนู "เครื่องมือแอดมิน" ในหน้า Google Sheet มีเครื่องมือเพิ่มอีก 5 อย่าง (v45, ปรับคำให้เข้าใจง่ายขึ้นใน v46):

- **เปิด/ปิดระบบย้ายข้อมูลเก่าไปแท็บสำรองอัตโนมัติ**: แทนที่จะต้องกดเมนู "ย้ายข้อมูลเก่าไปแท็บสำรอง" เองทุกครั้ง กดเมนู **"เปิดระบบย้ายข้อมูลเก่าไปแท็บสำรองอัตโนมัติ (รันเองทุกวันที่ 1 ของเดือน)"** ครั้งเดียวจะตั้ง trigger ให้ Apps Script รันย้ายข้อมูลทั้ง 3 ชีต (ActivityLogs/QualityScores/GeneratedPromptLogs) ให้เองอัตโนมัติทุกวันที่ 1 ของเดือน เวลาประมาณ 03:00 น. ไม่ต้องเข้ามากดเอง ดูผลย้อนหลังได้จากแท็บ `ActivityLogs` หัวข้อ `AUTO_ARCHIVE` ปิดได้ทุกเมื่อผ่านเมนู "ปิดระบบย้ายข้อมูลเก่าไปแท็บสำรองอัตโนมัติ" — **หมายเหตุ**: การตั้ง trigger ครั้งแรกอาจมีหน้าต่างขออนุญาต (authorize) เพิ่มเติมเหมือนตอนเชื่อม MailApp ครั้งแรก ให้กดอนุญาตตามปกติ
- **ล้างไฟล์รูปที่ไม่มีการอ้างอิงแล้วใน Drive**: ตอนลบ Prompt ที่บันทึกไว้ หรือลบชุดข้อมูลที่เคย Commit ระบบลบแค่แถวใน Sheet เท่านั้น ไม่ได้ลบไฟล์รูป Schema/ER Diagram ที่เคยแนบไว้ใน Drive ด้วย ไฟล์เหล่านี้จะค้างเป็น "ไฟล์กำพร้า" สะสมไปเรื่อยๆ กินพื้นที่ Drive โดยไม่มีใครใช้แล้ว เมนูนี้จะสแกนหาไฟล์ที่ไม่มีการอ้างอิงเหลืออยู่จริง (เทียบกับ `SavedPrompts`, `GeneratedPromptLogs` และ `GeneratedPromptLogs_Archive`) แล้วย้ายเข้าถังขยะ (Trash) — **ไม่ได้ลบถาวรทันที** ยังกู้คืนได้จากถังขยะ Drive ภายใน 30 วันถ้าพลาด
- **ดูสรุปขนาดข้อมูลปัจจุบันของแต่ละชีต**: โชว์จำนวนแถวปัจจุบันของทุกชีตหลัก (รวมแท็บสำรองที่ย้ายไปแล้ว) พร้อมเตือน ⚠️ ถ้าชีตไหนมีแถวเกิน 3,000 แถว ช่วยตัดสินใจเชิงรุกว่าควรย้ายข้อมูลเก่าออกตอนไหน แทนที่จะรอให้ระบบเริ่มช้าก่อนถึงจะรู้ตัว
- **ตรวจสอบข้อมูลผิดปกติในชีต Users**: สแกนหา username ซ้ำ และผู้ใช้ที่ยังไม่มีอีเมลผูกไว้ (จะกู้คืนรหัสผ่านเองผ่าน OTP ไม่ได้จนกว่า Super_Admin จะตั้งอีเมลให้) สรุปเป็นรายชื่อพร้อมเลขแถวให้เช็คตรงจุดได้เลย
- **ค้นหา/กรองข้อมูลสะดวกขึ้นโดยไม่ต้องเขียนโค้ดเพิ่ม**: Google Sheets มีเครื่องมือในตัวที่ใช้ได้กับทุกแท็บอยู่แล้ว ได้แก่ **Ctrl+F** (ค้นหาข้อความในชีตปัจจุบัน) และเมนู **ข้อมูล (Data) → มุมมองตัวกรอง (Filter views)** สำหรับตั้งเงื่อนไขกรอง/เรียงข้อมูลแบบไม่กระทบข้อมูลจริงและไม่ทับมุมมองของคนอื่น แนะนำให้แอดมินตั้ง Filter view ไว้ล่วงหน้าสำหรับแท็บที่เปิดดูบ่อย เช่น `Users` และ `ActivityLogs`

## สำรองข้อมูลทั้งไฟล์ + ตรวจสอบความพร้อมก่อน Deploy (v48)

- **สำรองข้อมูลทั้งไฟล์ (Backup)**: ต่างจาก "ย้ายข้อมูลเก่าไปแท็บสำรอง" ตรงที่อันนั้นย้ายข้อมูลบางส่วนไปแท็บอื่นในไฟล์เดิม ส่วนอันนี้**คัดลอกสเปรดชีตทั้งไฟล์ (ทุกแท็บ ทุกข้อมูล) ไปเป็นอีกไฟล์ต่างหาก** เก็บไว้ในโฟลเดอร์ Drive ชื่อ `TestDataSimulator_SpreadsheetBackups` ตั้งชื่อไฟล์ตามวันที่-เวลาที่สำรอง กันไว้เผื่อไฟล์หลักเสียหายหรือถูกลบโดยไม่ตั้งใจ กด **"สำรองข้อมูลทั้งไฟล์เดี๋ยวนี้"** เพื่อสำรองทันที หรือกด **"เปิดระบบสำรองข้อมูลอัตโนมัติ (รันเองทุกวันจันทร์)"** ให้รันเองทุกสัปดาห์เวลาประมาณ 04:00 น. โดยไม่ต้องกดเอง (ดูผลย้อนหลังได้จาก ActivityLogs หัวข้อ `AUTO_BACKUP`) ปิดได้ทุกเมื่อผ่านเมนู "ปิดระบบสำรองข้อมูลอัตโนมัติ" — เก็บไว้เฉพาะไฟล์สำรองล่าสุด **8 ชุด** เกินนี้ระบบลบตัวเก่าสุดทิ้งอัตโนมัติ (ไปถังขยะ Drive ไม่ใช่ลบถาวรทันที) กันโฟลเดอร์สำรองบวมไม่มีที่สิ้นสุด
- **ตรวจสอบความพร้อมก่อน Deploy**: กดเมนู **"ตรวจสอบความพร้อมก่อน Deploy"** เพื่อเช็ครวดเดียวว่าตั้งค่าที่จำเป็นครบหรือยัง ได้แก่ (1) ตั้งค่า `GEMINI_API_KEY` ใน Script Properties แล้วหรือยัง (2) มีชีตหลักครบทุกแท็บที่จำเป็นไหม (3) มีผู้ใช้ role `Super_Admin` อย่างน้อย 1 คนไหม (4) สิทธิ์การแชร์สเปรดชีตถูกตั้งเป็น "จำกัด" (Restricted) แล้วหรือยัง (เช็คผ่าน Drive API ให้อัตโนมัติ ไม่ต้องเข้าไปดูเองที่ปุ่มแชร์) พร้อมแจ้งสถานะระบบอัตโนมัติต่างๆ และเวอร์ชัน backend ปัจจุบันประกอบ — **ใช้ได้ทั้งตอนนี้และตอนย้ายไปสเปรดชีต/บัญชีจริงตอน Deploy โดยไม่ต้องแก้โค้ดเพิ่ม** เหมาะเป็นเช็กสุดท้ายก่อนส่งมอบงานให้บริษัทใช้จริง

## ขั้นตอนกู้คืนข้อมูลจากไฟล์สำรอง (Backup & Recovery Runbook)

เอกสารนี้ใช้เมื่อเกิดเหตุจริง เช่น สเปรดชีตหลักถูกลบ/ข้อมูลเสียหาย/แก้ไขผิดพลาดจนต้องย้อนกลับ ทำตามลำดับนี้:

1. **หาไฟล์สำรองล่าสุด**: เปิด Google Drive ของบัญชีที่ deploy ระบบนี้ไว้ → หาโฟลเดอร์ชื่อ `TestDataSimulator_SpreadsheetBackups` → ไฟล์สำรองเรียงชื่อตามวันที่-เวลาที่สำรอง (ใหม่สุดอยู่บนสุดถ้าเรียงตามวันที่แก้ไขล่าสุด) เลือกไฟล์ที่มีข้อมูลใกล้เคียงช่วงเวลาที่ต้องการกู้คืนที่สุด (ปกติเก็บไว้ล่าสุด 8 ชุด ถ้าเปิดระบบสำรองอัตโนมัติรายสัปดาห์ไว้ ควรมีไฟล์ให้เลือกย้อนหลังได้ถึงประมาณ 2 เดือน)
2. **สิทธิ์สั่งกู้คืน**: ควรเป็น Super_Admin หรือเจ้าของบัญชี Google ที่ deploy ระบบนี้เท่านั้นที่ตัดสินใจสั่งกู้คืน เพราะการกู้คืนจะกระทบผู้ใช้ทุกคนที่ใช้ระบบอยู่ในขณะนั้น (ข้อมูลที่เกิดขึ้นหลังจุดที่สำรองไว้จะหายไปถ้าทับไฟล์หลักตรงๆ)
3. **ตรวจสอบไฟล์สำรองก่อนใช้จริง**: เปิดไฟล์สำรองที่เลือกไว้ขึ้นมาดูก่อน (ไม่ต้องแก้อะไร แค่เปิดดู) เช็คคร่าวๆ ว่าแท็บครบ (`Users`, `ActivityLogs`, `GeneratedDatasets`, `SavedPrompts`, `GeneratedPromptLogs`, `ColumnSchemaConfig`, `QualityScores`) และมีข้อมูลอยู่จริงไม่ใช่ไฟล์เปล่า
4. **เลือกวิธีกู้คืนตามความรุนแรงของปัญหา**:
   - **ถ้าไฟล์หลักเสียหายทั้งไฟล์/ถูกลบ**: คัดลอกไฟล์สำรอง (File > Make a copy) ตั้งชื่อให้เหมือนไฟล์หลักเดิม แล้วต้องผูก Apps Script ใหม่ทั้งหมด (Extensions > Apps Script → วางโค้ด `Code.gs` ใหม่ → รัน `setupSheet` อีกครั้ง — แต่ระวัง `setupSheet` จะไม่ทับข้อมูลเดิมที่คัดลอกมาอยู่แล้ว เพราะใช้ `getOrCreateSheet_`/`setHeadersIfEmpty_` ที่ไม่ทำลายชีตที่มีข้อมูลอยู่แล้ว) จากนั้น deploy เป็น Web App ใหม่ (ขั้นตอนที่ 3) แล้วอัปเดต `BACKEND_URL` ใน `Final.html` ให้ชี้ไปที่ URL ใหม่
   - **ถ้าแค่บางแท็บ/บางแถวถูกแก้ผิดพลาด**: ไม่ต้องกู้คืนทั้งไฟล์ — เปิดไฟล์สำรอง คัดลอกเฉพาะแท็บหรือช่วงแถวที่ต้องการ (Ctrl+C) แล้วไปวางทับในไฟล์หลักที่ตำแหน่งเดิม (Ctrl+V) ระวังเรื่องแถวป้ายชื่อกลุ่ม (📅/📦) กับการจัดกลุ่ม (row grouping) ที่อาจไม่ติดมาด้วยตอน copy-paste ธรรมดา ต้องจัดกลุ่มใหม่เองถ้าจำเป็น
   - **ถ้าต้องการดูข้อมูลเก่าอย่างเดียวไม่ได้จะกู้คืนจริง**: เปิดไฟล์สำรองดูตรงๆ ได้เลยโดยไม่ต้องทำอะไรเพิ่ม ไม่กระทบไฟล์หลัก
5. **ตรวจสอบความถูกต้องหลังกู้คืน**: หลังกู้คืนเสร็จ ให้กดเมนู **"ตรวจสอบความพร้อมก่อน Deploy"** อีกครั้งเพื่อเช็ค API Key/ชีตครบ/มี Super_Admin/สิทธิ์แชร์จำกัด จากนั้นลอง login เข้าเว็บจริง 1 รอบ และกด **"ดูสรุปขนาดข้อมูลปัจจุบันของแต่ละชีต"** เทียบจำนวนแถวกับที่คาดไว้ว่าตรงกันหรือไม่
6. **แจ้งผู้ใช้ในทีม**: ถ้ามีการกู้คืนที่ทำให้ข้อมูลบางช่วงเวลาหายไป (เพราะเกิดขึ้นหลังจุดที่สำรองไว้) ควรแจ้งทีมให้ทราบว่าข้อมูลช่วงไหนอาจหายไปบ้าง เพื่อให้ generate/commit ข้อมูลที่จำเป็นซ้ำใหม่ได้ถ้าต้องการ

---

## ข้อจำกัด/ข้อควรทราบ

- **โควต้า Apps Script**: บัญชี Google ฟรีมีโควต้า URL Fetch ~20,000 ครั้ง/วัน และรันสคริปต์ได้สูงสุดครั้งละ 6 นาที เพียงพอสำหรับการใช้งานทดสอบ/ทีมเล็ก แต่ไม่เหมาะกับ production ที่มีผู้ใช้จำนวนมากพร้อมกัน
- **ต้นทุน API**: การกด "สร้างข้อมูลทดสอบ" 1 ครั้ง เรียก Gemini API 2 ครั้ง (สร้างข้อมูล + ตรวจทานเนื้อหา) ถ้าอยู่ในโควต้า Free Tier จะไม่มีค่าใช้จ่ายเลย แต่มีข้อจำกัดจำนวนคำขอต่อนาที/ต่อวัน ถ้าใช้งานถี่เกินไปจะต้องรอสักครู่ก่อนลองใหม่ (หรือผูกบัตรเพื่ออัปเกรดเป็น paid tier ในภายหลังหากต้องใช้งานหนักขึ้น)
- **ความปลอดภัยรหัสผ่าน**: backend เก็บรหัสผ่านแบบ salted SHA-256 hash ไม่ใช่ plaintext มี session token จริงที่หมดอายุอัตโนมัติ (10 นาที) พร้อม Auto Logout เมื่อไม่มีการใช้งาน มี login lockout กันการเดารหัสผ่านซ้ำๆ (ผิด 3 ครั้งล็อก 60 วินาที) และการกู้คืนรหัสผ่านต้องยืนยันตัวตนผ่าน OTP ทางอีเมลก่อนเสมอ (ไม่ใช่แค่รู้ username เฉยๆ) เหมาะกับการใช้งานภายในทีม ไม่แนะนำสำหรับระบบที่ต้องรับผู้ใช้สาธารณะจำนวนมากในระดับ production จริงจัง
- **คะแนนคุณภาพข้อมูลอาจเป็น "N/A"**: ถ้าขั้นตอนที่ AI ตรวจทานเนื้อหา (semantic check) เกิดข้อผิดพลาดทางเทคนิค (เช่น เรียก API ไม่สำเร็จ/parse ผลลัพธ์ไม่ได้) ระบบจะแสดงคะแนน `ตรงตามเงื่อนไข %` และ `ความน่าเชื่อถือของข้อมูล %` เป็น **"N/A"** แทนที่จะเดาเป็น 100% เพื่อไม่ให้ข้อมูลที่ใช้ประกอบการตัดสินใจคลาดเคลื่อน
- **จำนวนแถวต่อคำขอ**: จำกัดไว้ **300 แถวต่อครั้ง** (ค่าคงที่ `MAX_ROWS_PER_GENERATE` ใน `Code.gs`) — เดิมเคยตั้งไว้ที่ 1,000 แต่พบว่าสูงเกินกว่าที่งบประมาณคำตอบสูงสุดของโมเดล (`GEMINI_MAX_TOKENS = 16000`) จะรองรับได้จริงสำหรับตารางที่มีมากกว่า 2-3 คอลัมน์ ทำให้คำขอแถวเยอะๆ มักถูก AI ตัดคำตอบก่อนครบแล้ว fail อยู่ดี (เสีย execution time ไปฟรีๆ ก่อนรู้ผล) ปรับได้ในโค้ดทั้งฝั่งหน้าเว็บ (`Final.html`) และ backend (`Code.gs`) หากต้องการเปลี่ยนอีก — ควรปรับสองที่พร้อมกันเสมอเพราะไม่มีการซิงก์ค่าอัตโนมัติระหว่างกัน
